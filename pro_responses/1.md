Q: 
Read the attached files first and base your answer on them. If any recommendation conflicts with the attached files, call that out explicitly.

You are acting as a senior Kaggle time-series researcher reviewing my repo and current approach.

Your job is not to give generic ideas. Your job is to find the highest-leverage, leakage-safe ways to move my score materially upward, and to tell me if my offline setup is probably misaligned before suggesting more model complexity.

Hard constraints:
- Prediction at time t may only use information available up to t
- No preprocessing on test may use future test rows
- Do not use weight as a feature
- Treat feature_al as high leakage risk unless explicitly justified
- Be skeptical of target reconstruction tricks and leaderboard-chasing ideas
- Do not suggest anything that relies on future test rows or suspicious proxy leakage

Known setup:
- Row-level forecasting task with columns: code, sub_code, sub_category, horizon, ts_index
- Train ts_index: 1..3601
- Test ts_index: 3602..4376
- Horizons: 1, 3, 10, 25
- 89.12% of test rows are unseen at the (code, sub_code, sub_category) level
- 0% of test rows are unseen at the (code, sub_category, horizon) level
- So cold-start below the coarse group level is a major issue

Metric:
def weighted_rmse_score(y_target, y_pred, w):
    denom = sum(w * y_target**2)
    ratio = sum(w * (y_target - y_pred)**2) / denom
    clipped = min(max(ratio, 0.0), 1.0)
    return sqrt(1.0 - clipped)

Current status:
- Best exact offline score so far: about 0.145
- Best current path is a stacked train-only overlay on top of a smoothed stable-group baseline
- It combines:
  1. smoothed stable-group blend
  2. exact warm last-target rules
  3. recent-vs-full history delta on stable groups for horizons 10 and 25
  4. a narrow code-specific warm overlay
- Weak spots:
  - horizons 1 and 3
  - DPPUO5X2-related slices
  - fold 02 / fold 03 instability
- Feature-only residual branches have not helped much so far
- Honest forward CV and cold-start-aware validation are already in place

What I want from you:
1. First, tell me whether a score target near 0.8 seems plausible under this offline metric, or whether the huge gap suggests metric mismatch, validation mismatch, or fundamentally wrong problem framing.
2. Identify the 5 most likely bottlenecks in my current approach.
3. Propose the 10 highest-leverage next experiments, ranked.
4. For each experiment, give:
   - hypothesis
   - why it fits this data geometry
   - leakage risk
   - implementation difficulty
   - expected upside
   - what result would falsify it
5. Separate ideas into:
   - validation / metric corrections
   - leakage-safe feature / model ideas
   - warm-start / cold-start handling ideas
   - ensemble / post-processing ideas
6. Explicitly reject generic low-value ideas unless you can tie them to this exact setup.
7. Be concrete. Use the attached files, not generic Kaggle boilerplate.

Output format:
- Section A: Is 0.8 realistic under this setup?
- Section B: Top bottlenecks
- Section C: Ranked experiments
- Section D: Ideas I would avoid
- Section E: What I would try first in the next 3 runs

A:
## Section A: Is 0.8 realistic under this setup?

No. Under the repo’s working metric, your current best exact offline score is `0.1450306103`, which corresponds to an error ratio of about `0.97897`. A score of `0.8` would require that ratio to drop to `0.36`. On the current aggregate denominator of `180,332,606.72`, that means cutting weighted SSE from about `176.54M` to about `64.92M`, a reduction of about `111.62M`. Nothing in the attached experiments suggests leakage-safe headroom of that scale.  

The huge gap looks much more like a leaderboard/proxy mismatch than a “just add more model complexity” problem. The host warning in your prompt is explicit: unusually high public scores may be leakage-driven, public rank is a poor proxy for private rank, target-derived train features can inflate CV/public scores, and some hidden test points are ignored by the scorer. That makes very high public numbers compatible with a much lower honest offline score.  

I do **not** think the core framing is fundamentally wrong. The repo has already audited the task as row-level, confirmed that `89.12%` of test rows are unseen at `(code, sub_code)` while `0%` are unseen at `(code, sub_category, horizon)`, and moved to an exact concatenated-fold metric plus sequential-safe inference. That is the right backbone for this competition. The remaining setup issue is that your current 4-fold proxy is still materially warmer than test: the folds are about `75%` to `78%` unseen at `(code, sub_code)` versus `89.12%` in test, so warm-start overlays are still a bit easier offline than they are likely to be on test.   

Conflict call-out: the long-horizon plan keeps a public-score target around `0.8718`, but I would **not** use `0.8` to `0.87` as a credible offline planning target from here. Based on the attached files, that target is better treated as a public-LB aspiration from a leakage-contaminated regime, not as a realistic benchmark for the exact offline proxy you’ve built.  

## Section B: Top bottlenecks

1. **Your selector is still slightly warm-biased.** The proxy is good enough to guide safe work, but it is still less cold than test and blind to the host’s hidden ignored subset. That means small warm-row gains can still look better offline than they should.  

2. **The remaining error mass is concentrated, but your corrections are still too global.** The current best stack has only one narrow code-specific patch (`K7Y1TTAH / horizon=25`), yet the error analysis still shows large zero-score, high-denominator slices around `OSJL3A7Y` and `X9BZ68VQ`, especially on horizons `25` and `10`. The docs themselves point to those codes as the next likely warm-overlay targets.   

3. **Horizons 1 and 3 are still mostly unsolved.** The incumbent gains came from the stable-group/horizon blend, exact warm overlays, and a recent-vs-full delta on horizons `10` and `25`. The error analysis still identifies `1` and `3` as the weakest horizons, and the short-horizon residual branches only nudged score from roughly `0.11195` to the low `0.112s`.   

4. **Warm-start handling is the best current lever, but it is still too static.** A simple exact-group warm overlay lifted the baseline from `0.13708` to `0.13868`, then the recent-delta branch to `0.14399`, then one narrow K7 patch to `0.14503`, mostly by lifting the warm slice. But fold `03` warm-start still clips to `0.0`, so the warm expert is not yet properly gated or localized.    

5. **The feature/residual branch is still aimed at the wrong surface and isn’t trustworthy enough.** The repo explicitly kept residuals on top of `smoothed_stable_group_mean_pw100`, not the current stacked incumbent; the strict-OOF normalized replacement branch plateaued below the raw tri-slice branch; the feature-only `HistGradientBoostingRegressor` probe scored `0.0`; and the old posthoc residual-plus-horizon edge is still unresolved.    

## Section C: Ranked experiments

### Validation / metric corrections

**1. Harder selector and colder stress proxy.**
Hypothesis: some candidate gains are surviving because the main selector is still a bit too warm-friendly. Why it fits: your folds are `75%` to `78%` cold at `(code, sub_code)` versus `89.12%` in test, and the scorer also hides an ignored subset; the current incumbent still has fold-level brittleness, especially fold `03` warm. I’d keep `aggregate_overall_score` canonical, but add a secondary scorecard with `(a)` cold-only exact score, `(b)` fold `02+03` combined score, and `(c)` top-energy pathological slices. Leakage risk: none. Difficulty: low. Expected upside: indirect but very high leverage for model selection. Falsify it if the ranking of your top 3 branches stays unchanged under all three selectors.   

### Warm-start / cold-start handling ideas

**2. Code-specific, clipped recent-delta on horizons 10 and 25.**
Hypothesis: the recent-vs-full-history correction is directionally right, but one global adjustment is too blunt. Why it fits: the docs say horizons `10` and `25` are the highest-leverage drift slices, the recent-delta branch was the single biggest safe jump (`0.13868 -> 0.14399`), and the next open question in the repo is explicitly whether code-specific or clipped delta variants can stop fold `03` warm collapse without giving back fold `02`. Leakage risk: low, because the delta is train-prefix only at `(code, sub_category, horizon)`, which has full test coverage. Difficulty: medium. Expected upside: about `+0.002` to `+0.006`. Falsify it if no code-specific/clipped variant beats both `recent_delta_v1` and `k7_v1` on the canonical score while also improving the fold `02+03` stress score.   

**3. Expand exact warm overlays to `OSJL3A7Y` and `X9BZ68VQ` on horizons 25 and 10.**
Hypothesis: the K7 patch worked because the remaining miss pattern is slice-local, not global. Why it fits: the working log explicitly names `OSJL3A7Y` and `X9BZ68VQ` on horizons `25` and `10` as the next likely exact-group warm overlays, and the error analysis shows those slices carry large denominators while still scoring `0.0`. Leakage risk: low, because this stays in the same exact-group last-target framework that already improved score safely. Difficulty: low to medium. Expected upside: about `+0.001` to `+0.004` cumulative. Falsify it if alpha sweeps on those named slices fail to improve canonical score without reopening fold `02` damage.   

**4. Short-horizon stable-drift overlay for horizons 1 and 3.**
Hypothesis: `h1/h3` need a stable-group drift expert too, but probably with shorter windows, stronger clipping, and maybe a relative-change form instead of the raw additive delta that worked on `10/25`. Why it fits: the error analysis repeatedly says horizons `1` and `3` are the weakest, while your current successful drift branch ignores them. Leakage risk: low, because it remains train-prefix aggregation at stable coverage level. Difficulty: medium. Expected upside: about `+0.002` to `+0.005`. Falsify it if `h1/h3` slice scores do not improve on folds `02/03` or if folds `01/04` give back more than the hard folds gain.  

**5. Adaptive warm-overlay gating instead of fixed alpha tables.**
Hypothesis: the warm expert still has headroom, but fixed alphas by `sub_category/horizon` are too coarse. Why it fits: warm-only score moved from `0.07124` on the base blend to `0.09379` with the recent-delta stack and `0.09914` after the K7 warm patch, while cold score stayed unchanged across the K7 patch; that is a strong signal that a better warm gate exists. I’d gate on train-prefix support, exact-group recency, code, horizon, and maybe recent stability. Leakage risk: low. Difficulty: medium. Expected upside: about `+0.0015` to `+0.004`. Falsify it if adaptive gates cannot beat the simpler fixed-rule overlay under the canonical selector.   

**6. Cold-row specialist blend weights by horizon.**
Hypothesis: one global blend weight between stable-group mean and horizon mean is leaving cold-row performance on the table. Why it fits: the first major jump came from blending those two safe baselines, horizons are explicitly categorical groups, and the weakest-horizon table is very uneven. I would learn separate frozen weights by horizon, and optionally by `code` where sample size is large enough, but apply them only to cold rows. Leakage risk: low. Difficulty: low to medium. Expected upside: about `+0.0005` to `+0.0025`. Falsify it if forward-calibrated per-horizon weights collapse back to the current global blend or fail on the newest fold.  

### Ensemble / post-processing ideas

**7. Small forward-calibrated ensemble search over the safe family.**
Hypothesis: the remaining gains are still in combining complementary low-capacity experts, not in a bigger model family. Why it fits: the main score jumps in the repo came from convex blending and narrow overlays, while the feature/residual branches lagged. I would search only over the safe set `{smoothed_stable_horizon_blend, warm-overlay, recent-delta variants, code-specific warm variants}` and calibrate weights using older folds only. Leakage risk: low if weights are frozen forward in time. Difficulty: medium. Expected upside: about `+0.0005` to `+0.002`. Falsify it if no forward-calibrated ensemble beats the incumbent on both canonical and colder stress selectors.  

**8. Resolve the residual-plus-horizon blend discrepancy only if it reproduces cleanly.**
Hypothesis: there might still be a tiny safe ensemble edge in the residual-plus-horizon direction, but only if the old artifact discrepancy disappears under a clean path. Why it fits: the docs record an unresolved posthoc `raw_best + horizon_mean` edge above the clean rebuilt path. That is too small and too unresolved to trust today, but still worth one strict forward-calibrated check after the incumbent-based overlays are exhausted. Leakage risk: low only if all tuning is forward-only; otherwise high. Difficulty: medium. Expected upside: small, likely `< +0.002`. Falsify it if the clean rebuild remains below the incumbent or only wins under all-fold posthoc fitting.  

### Leakage-safe feature / model ideas

**9. Rebuild residuals against the current incumbent stack, not `smoothed_stable_group_mean_pw100`.**
Hypothesis: residual learning underperformed because it was correcting the wrong base predictor. Why it fits: the repo explicitly kept residuals on top of the older grouped baseline, yet the mainline has moved to a much better stacked incumbent. After the warm/delta overlays, the remaining residuals are smaller and more localized, which is where a second stage has a chance. Leakage risk: medium-low if strict OOF/prefix-only construction is preserved. Difficulty: medium-high. Expected upside: about `+0.001` to `+0.003`. Falsify it if incumbent-based residuals still plateau below the simple incumbent or only win under posthoc shrink tuning.  

**10. Split the short-horizon residual branch into separate `h1` and `h3` models, with `feature_al` still banned.**
Hypothesis: the current shared `h1+h3` residual branch is averaging two different error regimes. Why it fits: the docs explicitly ask whether the residual branch should split by horizon, `h1/h3` are the persistent weak slices, and the tri-slice residual family plateaued well below the current incumbent. Leakage risk: medium-low, because you already have a strict OOF residual framework and `feature_al` can stay excluded. Difficulty: medium. Expected upside: about `+0.0005` to `+0.002`. Falsify it if separate `h1` and `h3` residuals cannot beat the shared branch on the canonical score.  

## Section D: Ideas I would avoid

I would avoid a **generic all-feature tree/boosting sweep** right now. This conflicts with the plans file only in priority, not in principle: the plans list gradient boosting as a candidate direction, but the attached experiments already show a sampled feature-only `HistGradientBoostingRegressor` probe scoring `0.0`, and the docs explicitly deprioritize tiny safe feature sets because they did not beat zero.   

I would avoid **mainlining `feature_al` or any target-reconstruction trick**. The prompt and docs both flag `feature_al` as a likely target proxy, and the host explicitly warned that public scores may be leakage-driven and that target-derived train features can inflate CV/public scores. Until you can prove causality, it stays out.  

I would avoid **direct fine-group memorization** at `(code, sub_code, sub_category, horizon)` as a replacement baseline. That exact idea was already tried in spirit and clipped to `0.0` overall and `0.0` on the warm slice under the canonical proxy. With `89.12%` of test rows unseen below the stable level, that failure is structural, not cosmetic.  

I would avoid **posthoc all-fold blend tuning** or any “saved artifact says it’s better” shortcut. The repo already records an unresolved discrepancy between the old posthoc residual-plus-horizon materialization and the clean rebuilt path. Until that is explained with a forward-calibrated reproduction, it is not evidence.  

I would avoid **deep sequence models / transformers as the next main branch**. This is another place where I’m intentionally conflicting with the plans file’s broad candidate menu: the attached evidence says the useful signal is currently in stable-level aggregation, careful drift handling, and narrow warm overlays, not in feature-heavy global modeling. The current problem is slice geometry and robustness, not model capacity.  

I would avoid **any use of future test rows for scaling, rolling features, or grouped post-processing**, and of course avoid using `weight` as a feature. The prompt and sequential pipeline are already explicit here, and there is no score upside worth risking disqualification.  

## Section E: What I would try first in the next 3 runs

**Run 1:** add the colder stress selector, then sweep **code-specific/clipped recent-delta** variants on top of `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1`, starting with `OSJL3A7Y`, `X9BZ68VQ`, and `K7Y1TTAH` on horizons `10` and `25`. Success condition: beat `0.1450306103` while also improving the fold `02+03` stress score. This is the highest-upside branch that stays fully inside the repo’s proven safe pattern.  

**Run 2:** expand the **exact warm last-target overlays** for `OSJL3A7Y` and `X9BZ68VQ` on horizons `25` and `10`, with alpha sweeps and a hard “no fold-02 damage” rule. Success condition: raise aggregate warm-start score above `0.0991` without reducing cold-start score. The attached files explicitly point to these codes as the next likely wins.  

**Run 3:** build a **short-horizon stable-drift branch** for `h1/h3` before touching heavier residual/feature models again. I would try shorter windows, stronger clipping, and maybe relative deltas rather than raw additive deltas. Success condition: real lift on `h1/h3` slices in folds `02/03`, not just a cosmetic aggregate bump.  

The main disagreement I have with the attached planning docs is priority, not direction: I would **not** spend the next three runs on generic boosting, deep sequence models, or another broad residual rewrite. The file-backed evidence says the highest-leverage path is still tighter slice targeting on top of the current leakage-safe stacked incumbent.  
