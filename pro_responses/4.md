Here is the concrete diagnosis.

One correction matters first: under the repo’s metric, **higher score is better**, and clipping only happens through the underlying error ratio. On that convention, the uploaded summaries do **not** actually show successful local watchlist gains for the two non-promoted `horizon=25` branches. The working log calls them “improved,” but the recorded watchlist scores move the wrong way: `x9bz68vq_h25_core` is logged as `0.1108426 → 0.0991596 / 0.0900428`, and `osjl3a7y_long_core` as `0.1235206 → 0.1123323 / 0.1035016`. Under this metric, those are regressions, not gains.  

1. Which folds and aggregate slices offset the supposed local gains?

For the `X9BZ68VQ / horizon=25` core branch, the parent `..._pentaslice_beta_hi_v1` sits at `0.1451607523`, while the `a003` and `a005` probes fall to `0.1448311663` and `0.1445990007`. Fold `01` ticks up only trivially, `0.2453986 → 0.2454025 → 0.2454049`, but that is more than offset by fold `02`, `0.0969218 → 0.0963340 → 0.0959259`, fold `03`, `0.0750573 → 0.0735138 → 0.0724034`, and fold `04`, `0.1046891 → 0.1045745 → 0.1044916`. The colder combined block also worsens: `fold_02_03` falls from `0.0864672` on the parent to `0.0854633` on `a003`, and the log’s own watchlist numbers fall as quoted above.   

For the `OSJL3A7Y / horizon=25` long-core branch, the same pattern is even cleaner. The `a003` and `a005` probes fall to `0.1446687857` and `0.1443124060`. Almost all of that loss comes from fold `02`, `0.0969218 → 0.0954998 → 0.0944696`, and fold `03`, `0.0750573 → 0.0732530 → 0.0719655`; fold `01` and fold `04` are nearly flat. Again, the logged watchlist numbers themselves fall, `0.1235206 → 0.1123323 / 0.1035016`.  

So the real offset is not some unrelated slice elsewhere. It is the same colder region the repo already treats as the danger zone: folds `02` and `03`, especially their warm-start behavior. That is consistent with the earlier error-analysis context, which already identified horizons `1` and `3` as the broad weak area and folds `02` and `03` as the persistent collapse zone. 

2. Is this mostly denominator concentration, collateral regressions elsewhere, or clipping?

It is **mostly narrow warm-slice regression on the touched `horizon=25` rows**, with denominator concentration acting as an amplifier and clipping acting as a mask.

It is **not** broad collateral elsewhere. In both exact X9 and OSJL fold tables, the cold-start scores and cold-start error sums are unchanged across parent and child; the overlays are effectively touching only warm rows, and the damage shows up there. Horizons `1`, `3`, and `10` are not the place where these branches move.  

Denominator concentration still matters because `horizon=25` carries `94.88M` of the total `180.33M` denominator, a bit over half of total aggregate weight. Small numerator regressions there matter. But that is only the amplifier, not the root cause. The exact watchlists themselves are much smaller, about `7.03M` denominator for `x9bz68vq_h25_core` and `9.74M` for `osjl3a7y_long_core`, and those slices still get worse. If they had truly improved, they would have helped.    

Clipping is a major part of why this branch can look deceptively okay in score-only diagnostics. The metric clips once `error_sum / denom_sum` exceeds `1.0`. On the parent, the combined `fold_02_03` warm-start slice is already clipped to `0.0`, with warm-start error `27,349,179.91` above warm-start denominator `27,289,102.73`. The X9 `a003` probe pushes that warm-start error even higher, to `27,365,486.75`, while the displayed warm-start score stays `0.0`. That is the key masking behavior: extra damage can be real in the raw numerator even when the clipped score no longer moves.   

So the ranking is: **first, local warm H25 regression; second, denominator concentration; third, clipping hides the size of the damage**.

3. What does this imply for future branch design?

Within this exact family, the bar has to be stricter than “the watchlist looked promising.” The repo already says `aggregate_overall_score` is the canonical selector and that `fold_02_03` plus long-horizon watchlists are veto diagnostics. For this family, that should be read very literally: a long-horizon warm overlay is only alive if it improves canonical aggregate, does not worsen `fold_02_03`, and lowers raw warm-start error on the exact touched H25 slice. These X9/OSJL H25 probes fail that test. 

The broader implication is that the remaining aggregate leverage was not in widening more H25 warm bundles. The saved error-analysis context still showed the broad weak area in short horizons and colder folds, and the only later promotion came from the `h=3` beta-up follow-up, which improved `aggregate_overall_score`, `aggregate_cold_start_score`, `aggregate_warm_start_score`, and `fold_02_03` while leaving the long-horizon watchlists unchanged. That is a strong sign that same-family H25 warm overlays were already giving lower-value and less-robust moves than the short-horizon correction path.   

A smaller but important implication is diagnostic: for any future revisit of this family, raw `error_sum` deltas matter more than clipped scores on already-bad warm subsets. Otherwise you can mistake “score stayed at 0.0” for “no real harm.”

4. Does this argue for stopping further long-horizon warm-overlay work entirely?

For **this same warm-overlay program**, yes.

The bundle readme says the long-horizon warm-overlay family now looks plateaued and that this is the right point to stop asking for more micro-tuning in the same family. The working log is even more explicit: the `NQ58FVQM`-only retry, the `X9BZ68VQ / horizon=25` core retries, and the `OSJL3A7Y / horizon=25` long-core retries all regressed overall versus the promoted parent, and the immediate next-step guidance is to treat that branch as plateaued for now and prefer materially different work over more exact-CV knob sweeps.   

So my answer is: **stop further same-family long-horizon warm-overlay micro-work**. The uploaded evidence does **not** support continuing to widen or retune these H25 warm bundles. Reopen that area only if the mechanism is materially different and can clear the same aggregate-plus-cold-stress gates; otherwise, the current files already argue that the branch is exhausted.
