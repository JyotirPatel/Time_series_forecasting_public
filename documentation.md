# Working Log

This file is the shared memory and audit log for the long-horizon run.

## Snapshot

- Prepared on: `2026-04-09 UTC`
- Repo state at latest update: docs, a repo-local `.venv` bootstrapped from `requirements.txt`, runnable Python scaffold, deterministic forward CV tooling, canonical exact aggregate scoring, a machine-readable evaluation `stress` block, a shared baseline registry, clipped/gated recent-delta variant configs for horizons `10` and `25`, exact real-data delta-sweep outputs, narrow OSJL/X9 warm-probe configs, short-horizon `h=1/3` drift-overlay configs, saved residual-model artifacts on top of the smoothed stable-group baseline, a strict-OOF normalized residual branch with incumbent-targeting defaults plus stress diagnostics, a rebuilt fixed-prefix residual correction path, a reusable normalized residual inference / prediction path for the promoted `h=1/3` winner, a conditional expert blend path for routed specialists, registry-driven required-column resolution for feature-using baselines, whole-frame static prediction fast paths for opt-in predictors, streamed exact forward-CV evaluation, stable order-aware residual sampling, reusable warm-start / recent-delta overlay predictors that now run end to end for evaluation and test inference, a causalized LightGBM residual branch on top of the promoted ridge incumbent, fresh causal LGBM forward-CV artifacts, a checked-in calibrated `scripts/predict_lgbm.py` submission path for the all-horizon residual winner, horizon-scoped warm-gating in the evaluator and predictor, a durable submission-style tail proxy, live public-score recovery from `0.0000` to `0.2045`, a promoted `warmh13` public winner at `0.2099`, a closed routed-expert retry at `0.1990`, a private GitHub remote prepared for Windows-native handoff, a GPT-5.4 Pro recommendation handoff captured in the project docs, and the current cloud host details below; `data/train.parquet` and `data/test.parquet` are restored on this host, authenticated Kaggle token access works, five `warmh13` experiments were run with `msg015` narrowly best (`0.1605824443` overall vs control `0.1605767426`), and the current bottleneck is the incumbent OOF path plus low parallelism rather than CPU quota
- Current status: bootstrap scaffold completed, competition semantics audited, cold-start-aware validation implemented, canonical offline scoring fixed, the direct warm-start sanity check still rejects naive fine-group replacement, `data/train.parquet` and `data/test.parquet` are restored on this host, `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1` survived that veto pass, the first long-horizon and later `h=3` OSJL/X9 warm probes both regressed, widening the short-horizon `h=1/3` drift overlay to all five sub-categories produced a new exact winner, the pentaslice `X9BZ68VQ / horizon=25` core and `OSJL3A7Y / horizon=25` long-core warm-overlay probes also regressed their own targeted watchlists once the higher-is-better metric was rechecked, the exact evaluator hot path is now faster and less memory-hungry without changing scores, the first conservative incumbent-based strict-OOF residual branch on horizons `1` and `3` survived the canonical and stress gates, the follow-up longwatch residual on `OSJL3A7Y` / `X9BZ68VQ` horizons `10` and `25` regressed badly and is now closed, the promoted `h=1/3` residual family now has a deterministic registry / prediction path, the feature-loading bug that left residual registry baselines with only `ts_index` is fixed, the reusable ridge winner still exactly matches the promoted strict-OOF score under `scripts/evaluate_baselines.py`, the routed longwatch specialist and conditional blend still lose after the corrected rerun, authenticated Kaggle API access is now unblocked, the old LightGBM residual summaries were found to rely on a fold-future `sub_code_is_seen` leak, that leak is now repaired, the causal LightGBM `direct` branch has been demoted, residual horizons plus trainer-budget and pruning knobs are now configurable in the evaluator, the stale ad hoc LGBM submission path has now been replaced by a reproducible calibrated script, the all-horizon warm-only and routed-expert retries are both now closed, and the active center is the `warmh13` causal weighted LightGBM residual family on top of `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1`
- Current best exact offline score: `0.1605824443` from `outputs/lgbm_models_causal_20260413/forward_cv_h360_f4_s360_code_sub_code/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_warmh13_summary.json`; the closest control in the same sweep is `0.1605767426`
- Current best verified public score from a checked-in pipeline: `0.2099` from `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_warmh13_submission.csv`, generated by `scripts/predict_lgbm.py`
- Recommended current milestone: `Milestone 5: Failure Analysis And Self-Improvement`
- Long-run autonomy target: keep iterating unattended toward stronger leakage-safe offline performance, treating public target `0.8718` as a leaderboard reference rather than a credible offline score target under the current proxy

## Windows-Native Handoff (`2026-04-10 UTC`)

- GitHub repo: `https://github.com/meettilavat/Time_series_forecasting`
- Latest pushed branch: `main`
- Latest pushed commit at handoff: `6e062f8` (`Initial project import`)
- Validation run before push: `. .venv/bin/activate && pytest -q -s tests`
- Validation result before push: `24 passed in 2.72s`
- Post-handoff progress on the Windows checkout:
  - `data/train.parquet` and `data/test.parquet` are present in this checkout
  - the exact clipped/gated delta sweep is saved under `outputs/advanced_models/delta_sweep_exact_20260410_code_sub_code`
  - the secondary triplet confirmation sweep is saved under `outputs/advanced_models/delta_sweep_triplet_20260410_shortlist`
  - the current promoted winner is `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1` at `0.1453567994`
  - a ChatGPT upload bundle with renamed, ordered source files is prepared under `chatgpt_upload_bundle_20260410_plateau_review`
- Exact resume point:
  - canonical incumbent base remains `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1` at `0.1451839276`, but the current best exact offline branch is now `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1` at `0.1453567994`
  - keep `aggregate_overall_score` as canonical
  - treat the `stress` block as a veto / robustness report, not as a replacement selector
  - clipped/gated recent-delta variants for horizons `10` and `25` were exact-rerun and are currently demoted behind the incumbent path
  - the long-horizon warm-overlay family is exhausted for now; the `NQ58FVQM`-only, `X9BZ68VQ / horizon=25` core, and `OSJL3A7Y / horizon=25` long-core retries all regressed overall versus the promoted parent, and later review confirmed the targeted watchlist scores also moved the wrong way under the repo metric
- Immediate next actions on the Windows-native checkout:
  - treat the short-horizon `h=1/3` drift family as near-plateaued after the successful pentaslice `h=3` beta-only follow-up; the next test in that area must be a materially different residual model, not another overlay knob sweep
  - keep the long-horizon warm-overlay branch closed for now: the `NQ58FVQM`-only, `X9BZ68VQ / horizon=25` core, and `OSJL3A7Y / horizon=25` long-core retries regressed both canonical score and their own targeted watchlists under the repo metric
  - keep the evaluator/runtime refactor in place: whole-frame static prediction, fold streaming, and one-pass stress assembly preserved exact scores and should remain the default exact-CV path
  - treat `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1` as the new best offline branch; it improved canonical score, `aggregate_cold_start_score`, `aggregate_warm_start_score`, `fold_02_03`, and horizons `1` / `3` without damaging the long-horizon watchlists
  - keep the longwatch residual ridge branch closed for now: `ridge_oofnorm_longwatch_h1025_s003_q80_a100_unweighted_v1` regressed canonical score, colder diagnostics, and its own watchlist slices
  - the reusable registry winner `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1` now matches the promoted strict-OOF result exactly under `scripts/evaluate_baselines.py` at `0.1453567994` after fixing registry-driven feature loading; treat the older lower-scoring registry outputs as superseded
  - keep both `winner_longwatch_residual_raw_v1` and `winner_plus_longwatch_expert_blend_v1` demoted even after the corrected feature-loading rerun: the specialist fell to `0.1380775138` and the routed blend only reached `0.1446841822`, while both still lost on canonical score, `fold_02_03`, and the longwatch watchlists
  - if authenticated submission access returns, the deterministic packaging path now exists for both the reusable winner and the routed blend through `scripts/predict_baseline.py`

## Overnight Update (`2026-04-11 UTC`)

- Branch in progress: `codex/lgbm-causal-residual`
- Kaggle status check:
  - `mcp__kaggle__get_competition` confirms the competition deadline remained `2026-04-07T07:00:00Z`
  - unauthenticated leaderboard access via `mcp__kaggle__get_competition_leaderboard` returned `Unauthenticated`
  - treat this run as offline-only unless authenticated submission access appears later
- Root-cause audit on the saved LightGBM branch:
  - the saved `lgbm_residual_weighted_v2` artifact at `0.1457445423` initially looked like the repo leader
  - code review showed `scripts/evaluate_lgbm.py` was building `sub_code_is_seen` from full-train metadata once and reusing it for every fold, which leaked future fold membership into early-fold validation
  - this did not fully explain away the branch, so the correct action was to repair causality and rerun rather than discard the family
- Code changes made:
  - added `build_seen_sub_code_pairs()` to `src/ts_forecasting/lgbm_models.py`
  - switched `scripts/evaluate_lgbm.py` to use fold-local seen `(code, sub_code)` pairs instead of full-train pairs
  - switched the LightGBM stacked/residual incumbent from `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1` to the promoted ridge winner `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1`
  - added `tests/test_evaluate_lgbm.py` to lock both behaviors
- Validation after the code change:
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests/test_evaluate_lgbm.py`
  - result: `2 passed in 1.15s`
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests`
  - result: `61 passed, 1 warning in 1.94s`
- Fresh causal LightGBM rerun:
  - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411`
  - output: `outputs/lgbm_models_causal_20260411/forward_cv_h360_f4_s360_code_sub_code/lgbm_residual_weighted_v2_summary.json`
  - aggregate score: `0.1458942481`
  - aggregate cold-start score: `0.1605631097`
  - aggregate warm-start score: `0.1016897279`
  - versus the promoted ridge winner:
    - overall: `+0.0005374486`
    - cold-start: `+0.0002563015`
    - warm-start: `+0.0016727471`
    - combined `fold_02_03`: `+0.0011426836`
    - horizon `1`: `+0.0099325970`
    - horizon `3`: `+0.0066042347`
    - horizons `10` and `25`: unchanged
    - watchlists `osjl3a7y_long_core`, `x9bz68vq_h10_core`, and `x9bz68vq_h25_core`: unchanged
- Current interpretation:
  - the causal LightGBM residual branch is now the repo leader and appears to improve exactly the short-horizon region that remained weakest in the ridge family
  - long-horizon pain points are still open, but this family does not make them worse under the current exact proxy
  - the next best use of overnight budget is a small set of logged causal LGBM residual sweeps around this winner, not a return to same-family longwatch warm overlays
- First causal LightGBM sweep batch:
  - direct weighted check:
    - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode direct --output-dir outputs/lgbm_models_causal_20260411`
    - result: `lgbm_direct_weighted_v2` scored `0.0964218059`
    - interpretation: close the direct branch for now; raw feature LGBM is not competitive without the incumbent scaffold
  - residual sweep `leaf31_lr003_mc400_ff07_bf07_ra05_rl2`:
    - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411 --name-suffix leaf31_lr003_mc400_ff07_bf07_ra05_rl2 --num-leaves 31 --learning-rate 0.03 --min-child-samples 400 --feature-fraction 0.7 --bagging-fraction 0.7 --bagging-freq 1 --reg-alpha 0.5 --reg-lambda 2.0`
    - result: `0.1461513804`
    - interpretation: better than the causal base by `+0.0002571323`, improving `h1` and `h3` while leaving `10/25` and the long-horizon watchlists unchanged
  - residual sweep `leaf31_lr002_mc400_ff07_bf07_ra05_rl2`:
    - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411 --name-suffix leaf31_lr002_mc400_ff07_bf07_ra05_rl2 --num-leaves 31 --learning-rate 0.02 --min-child-samples 400 --feature-fraction 0.7 --bagging-fraction 0.7 --bagging-freq 1 --reg-alpha 0.5 --reg-lambda 2.0`
    - result: `0.1462223717`
    - interpretation: current repo leader; better than the prior sweep and the causal base, again through `h1/h3` only
  - residual sweep `leaf31_lr002_mc300_ff07_bf07_ra05_rl2`:
    - result: `0.1457990876`
    - interpretation: relaxing the child floor gave back too much; keep `min_child_samples=400`
  - residual sweep `leaf63_lr002_mc400_ff07_bf07_ra05_rl2`:
    - result: `0.1461361535`
    - interpretation: restoring more leaf capacity did not beat the tighter `leaf31` variant; keep the smaller-leaf setting for the next batch
  - residual horizon override work:
    - added CLI support for `--residual-horizons` plus tests in `tests/test_evaluate_lgbm.py`
    - `h1`-only check `lgbm_residual_weighted_v2_h1_leaf31_lr002_mc400_ff07_bf07_ra05_rl2` scored `0.1456093085`
    - `h3`-only check `lgbm_residual_weighted_v2_h3_leaf31_lr002_mc400_ff07_bf07_ra05_rl2` scored `0.1459445893`
    - interpretation: both single-horizon branches lose to the combined `h1+h3` residual path, so keep both short horizons active
  - residual sweep `leaf31_lr002_mc400_ff06_bf07_ra05_rl2`:
    - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411 --name-suffix leaf31_lr002_mc400_ff06_bf07_ra05_rl2 --num-leaves 31 --learning-rate 0.02 --min-child-samples 400 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --reg-alpha 0.5 --reg-lambda 2.0`
    - result: `0.1463118287`
    - interpretation: new repo leader; lowering `feature_fraction` improved canonical score, cold-start score, and still left long-horizon slices unchanged
  - residual sweep `leaf31_lr002_mc400_ff06_bf06_ra05_rl2`:
    - result: `0.1461210682`
    - interpretation: lowering `bagging_fraction` to `0.6` gave back score, so keep `bagging_fraction=0.7`
  - all-horizon breakout:
    - `h10`-only `lgbm_residual_weighted_v2_h10_leaf31_lr002_mc400_ff06_bf07_ra20_rl4` jumped to `0.1470752540`, showing long-horizon residual modeling was viable rather than closed
    - stacking all horizons with `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc400_ff06_bf07_ra20_rl4` jumped again to `0.1511750576`
    - moving that all-horizon branch to stronger regularization produced the current frontier:
      - `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc400_ff06_bf07_ra20_rl6`: `0.1539577071`
      - `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc500_ff06_bf07_ra20_rl6`: `0.1578030276`
      - `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc500_ff06_bf07_ra20_rl5`: `0.1578436240`
      - `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_ff06_bf07_ra20_rl5`: `0.1591594218`
    - rejected nearby variants:
      - `mc480`: `0.1566734297`
      - `mc600`: `0.1551632535`
      - `lr0.015` on `mc500/rl6`: `0.1518423419`
      - `ff07`: `0.1495662563`
      - `lr0.025`: `0.1485848392`
      - `mc550`: `0.1500760243`
      - `ra25/rl5`: `0.1577198399`
    - interpretation update:
      - raising `min_child_samples` from `500` to `520` gave another clean gain, so the current search center is now the `mc520/ra20/rl5` variant rather than the earlier `mc500` winner
    - stress read on the `mc500/ra20/rl5` winner:
      - strong canonical lift versus the earlier short-horizon frontier
      - improved cold-start and warm-start aggregates
      - the long-horizon watchlists improved enough to keep this branch open
      - the all-horizon branch is now the primary optimization target
  - next suggested batch:
    - stay near the new all-horizon winner: `learning_rate=0.02`, `num_leaves=31`, `min_child_samples=520`, `feature_fraction=0.6`, `bagging_fraction=0.7`, `reg_alpha=2.0`, `reg_lambda=5.0`, residual horizons `1 3 10 25`
    - if another session continues from here, vary one axis at a time from this winner rather than reopening direct/stacked or warm-overlay branches
  - follow-up `min_child_samples` bracket around the `mc520` winner:
    - `mc510`: `0.1544066981`
    - `mc515`: `0.1547989708`
    - `mc525`: `0.1573962948`
    - `mc530`: `0.1571221864`
    - interpretation: the local optimum is sharp and still centered at `mc520`; `mc530` modestly helped `x9bz68vq_h10_core` but did not recover enough overall or on the other veto slices to replace the winner
  - trainer-budget surface:
    - added CLI support for `--n-estimators` and `--early-stopping-rounds` in `scripts/evaluate_lgbm.py`, with tests in `tests/test_evaluate_lgbm.py`
    - validation after this change:
      - `& .\.venv\Scripts\python.exe -m pytest -q -s tests/test_evaluate_lgbm.py`
      - result: `8 passed in 1.16s`
      - `& .\.venv\Scripts\python.exe -m pytest -q -s tests`
      - result: `67 passed, 1 warning in 1.89s`
    - budgeted control rerun:
      - `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_ff06_bf07_ra20_rl5_ne6000_es150`: `0.1591594218`
      - interpretation: the current winner was not materially limited by the old `2000` / `50` training budget; the wider budget reproduced the exact same aggregate score
    - fair low-learning-rate retry:
      - `lgbm_residual_weighted_v2_h131025_leaf31_lr0015_mc520_ff06_bf07_ra20_rl5_ne6000_es150`: `0.1523626336`
      - interpretation: even after removing the old budget handicap, the lower-learning-rate branch still regressed badly, so `lr=0.015` is now lower confidence around this frontier
  - pruning-knob surface:
    - added CLI support for `--max-depth`, `--min-sum-hessian-in-leaf`, and `--min-split-gain` in `scripts/evaluate_lgbm.py`, with tests extended in `tests/test_evaluate_lgbm.py`
    - validation after this change:
      - `& .\.venv\Scripts\python.exe -m pytest -q -s tests/test_evaluate_lgbm.py`
      - result: `8 passed in 1.16s`
      - `& .\.venv\Scripts\python.exe -m pytest -q -s tests`
      - result: `67 passed, 1 warning in 1.90s`
    - `max_depth` sweep on the `mc520` winner:
      - `md6`: `0.1571118114`
      - `md8`: `0.1529901298`
      - interpretation: explicit depth caps are not the right regularizer for this branch
    - `min_sum_hessian_in_leaf` sweep on the `mc520` winner:
      - `hess10`: `0.1583259864`
      - `hess50`: `0.1586328022`
      - interpretation: both stayed close but neither recovered enough overall or on the watchlists to replace the incumbent
    - `min_split_gain` sweep on the `mc520` winner:
      - `msg001`: `0.1591596544`
      - `msg005`: `0.1591596686`
      - `msg010`: `0.1591612622`
      - `msg020`: `0.1591598634`
      - interpretation: a small positive split penalty is beneficial here, with `min_split_gain=0.1` currently the best verified setting; it also slightly improved `aggregate_cold_start_score`, `aggregate_warm_start_score`, and `osjl3a7y_long_core` without worsening the other tracked veto slices
  - second-stage refinement from the split-gain winner:
    - local `min_split_gain` refinement around the original `mc520/ra20/rl5` center:
      - `msg008`: `0.1591596544`
      - `msg009`: `0.1591596352`
      - `msg011`: `0.1591594218`
      - `msg012`: `0.1591605521`
      - interpretation: `min_split_gain=0.1` remained the local peak on that original regularization slice
    - interaction checks:
      - `hess10 + msg010`: `0.1583309577`
      - `hess50 + msg010`: `0.1587240693`
      - interpretation: the Hessian-floor interaction remained worse than the simpler split-gain winner
    - L2 refinement around the split-gain winner:
      - `ra20/rl4/msg010`: `0.1600067320`
      - `ra20/rl45/msg010`: `0.1591238929`
      - `ra20/rl6/msg010`: `0.1583638616`
      - `ra20/rl35/msg010`: `0.1570088357`
      - interpretation: lighter L2 at `reg_lambda=4.0` produced a meaningful jump, while both stronger and weaker nearby values lost
    - L1 refinement on top of `rl4/msg010`:
      - `ra15/rl4/msg010`: `0.1585624424`
      - `ra25/rl4/msg010`: `0.1600104021`
      - `ra275/rl4/msg010`: `0.1599198761`
      - `ra30/rl4/msg010`: `0.1599375521`
      - interpretation: `reg_alpha=2.5` is the local peak on that ridge
    - re-opened `min_child_samples` check under the new regularization mix:
      - `mc525/msg010/ra25/rl4`: `0.1574223644`
      - `mc530/msg010/ra25/rl4`: `0.1572863507`
      - interpretation: the earlier `mc520` optimum still holds even after the regularization shift
    - latest split-gain refinement on the new `ra25/rl4` center:
      - `msg015`: `0.1600286243`
      - `msg020`: `0.1600321380`
      - `msg025`: `0.1600286643`
      - interpretation: the current best verified config is now `min_split_gain=0.2`, `reg_alpha=2.5`, `reg_lambda=4.0` on top of the same `mc520` all-horizon residual center; it improved canonical, cold-start, and warm-start aggregates while keeping `fold_02_03` flat and lifting `x9bz68vq_h25_core` relative to the earlier `ra25/rl4` center
    - post-`msg020` micro-tuning:
      - `rl410`: `0.1600343060`
      - `rl425`: `0.1589152438`
      - `rl405`: `0.1600304541`
      - `rl408`: `0.1600435435`
      - `rl409`: `0.1600328150`
      - `rl407`: `0.1600266372`
      - `mc522` on the `rl410` center: `0.1544155495`
      - interpretation: the live ridge is extremely narrow around `reg_lambda ~= 4.08`; `rl408` is the current best verified point and also improved `fold_02_03` versus `rl410`, while `mc522` was a hard rejection that re-confirms `mc520` as the active optimum

## Submission Path Fix (`2026-04-11 UTC`)

- Root-cause investigation after the first live submit:
  - `submission.csv` had valid `id,prediction` schema, matched `test.parquet` ids exactly, and was accepted by Kaggle
  - the initial public `0.0000` therefore pointed to prediction generation, not CSV formatting
  - local reproduction on the last fold showed that a full-train refit without an evaluation holdout can collapse the score to `0.0`, while the calibrated `718`-tree refit matches the fold-eval result exactly
- Code changes made:
  - added `scripts/predict_lgbm.py` as the checked-in residual LightGBM submission path
  - the script calibrates `n_estimators` on a causal holdout (`ts_index 3242..3601`) before the final full-train refit, rather than silently using the training default
  - removed the dead `sub_code_is_seen` feature from `scripts/evaluate_lgbm.py`; it had become a train-constant column and was not contributing signal to the fitted residual branch
  - added `tests/test_predict_lgbm.py` to lock:
    - explicit/calibrated `n_estimators` resolution,
    - the incumbent-only feature surface for prediction,
    - standalone script bootstrap via `--help`,
    - calibration use of the evaluator's OOF defaults
- Validation after the fix:
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests/test_predict_lgbm.py`
  - result: `6 passed`
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests`
  - result: `73 passed, 1 warning`
- Corrected submission generation:
  - command:
    - `& .\.venv\Scripts\python.exe scripts/predict_lgbm.py --train-path data/train.parquet --test-path data/test.parquet --output outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_submission.csv --residual-horizons 1 3 10 25 --num-leaves 31 --learning-rate 0.02 --min-child-samples 520 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --min-split-gain 0.2 --reg-alpha 2.5 --reg-lambda 4.08`
  - calibration metadata:
    - `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_submission_metadata.json`
    - calibrated `best_iteration`: `718`
  - the corrected artifact differs materially from the stale ad hoc `submission.csv`:
    - changed rows: `1,445,607`
    - mean absolute prediction difference: `0.9881`
    - max absolute prediction difference: `31.0433`
  - kept the corrected artifact under `outputs/submissions/`; the temporary root-level `submission.csv` used during debugging is not part of the source-controlled pipeline
- Live Kaggle result:
  - stale ad hoc path:
    - message: `codex rl408 residual public test`
    - public score: `0.0000`
  - corrected checked-in path:
    - message: `codex calibrated residual fix`
    - public score: `0.2045`
- Interpretation:
  - the previous `0.0000` was caused by the ad hoc LGBM submission-generation path, not by the CSV schema
  - the checked-in calibrated script is now the only submission path that should be trusted for this LightGBM residual family
  - offline/public alignment is still incomplete because `0.1600` offline and `0.2045` public remain far from the leaderboard target, but the pipeline is now reproducible and no longer catastrophically wrong
- Next step:
  - use the fixed `scripts/predict_lgbm.py` path for all future LGBM submissions
  - re-open evaluation/proxy work with live-score feedback, instead of guessing from ad hoc artifacts

## Environment

- Default Python environment: repo-local `.venv`
- Dependency source of truth: `requirements.txt`
- Hardware:
  - GPU: `NVIDIA RTX A5000` with `24564 MiB` VRAM
  - RAM: `251 GiB` visible to the OS
  - CPU: `AMD EPYC 7713P` with `128` logical CPUs
  - Storage: `WD_BLACK SN850X`
- Bootstrap commands:
  - `python -m venv .venv`
  - `. .venv/bin/activate`
  - `python -m pip install -r requirements.txt`
- Current installed baseline stack in `.venv`:
  - `duckdb==1.5.1`
  - `numpy==2.4.4`
  - `pandas==3.0.2`
  - `pyarrow==23.0.1`
  - `pytest==9.0.3`
  - `scikit-learn==1.8.0`
- Runtime note: on this many-core host, LightGBM now caps auto `num_threads` at `64` and enables `force_col_wise`, so CPU-bound training should scale much better than before but still not linearly across all logical cores.

## Competition Facts Captured At Setup

- Competition: `Hedge fund - Time series forecasting`
- Slug: `ts-forecasting`
- Public description: `Time series forecasting by code, sub-code, sub_category and horizon`
- Metric label: `Skill Score`
- Deadline: `2026-04-07 07:00 UTC`
- Private leaderboard release: `2026-04-14 07:00 UTC`
- Public leaderboard share: `24%`
- Max daily submissions: `1`
- Row id column: `id`
- Total scored rows: `1,447,107`

## Important Assumptions

- The competition deadline appears to have passed before this repo was prepared.
- The next session should verify competition accessibility, rules, and evaluation details before making metric-sensitive decisions.
- The train/test split appears to be a forward time split based on `ts_index` continuity; this is an inference from the local data and should be confirmed in the competition rules.
- User-supplied competition text provides the current working definition of the metric and sequential rules.
- The prediction unit is row-level because both splits have one unique `id` and one unique `(code, sub_code, sub_category, horizon, ts_index)` key per row.
- `data/train.parquet` and `data/test.parquet` are restored on this host; data-dependent commands can run again without the earlier mount blocker.

## Local Dataset Inventory

- Files present:
  - `data/train.parquet`
  - `data/test.parquet`
- Shapes:
  - train: `5,337,414 x 94`
  - test: `1,447,107 x 92`
- Shared columns:
  - identifiers/grouping: `id`, `code`, `sub_code`, `sub_category`, `horizon`, `ts_index`
  - features: `86` columns from `feature_a` through `feature_ch`
- Train-only columns:
  - `y_target`
  - `weight`
- Column semantics from the user-provided rules:
  - `id` is the concatenation of `code`, `sub_code`, `sub_category`, `horizon`, and `ts_index` joined by `__`
  - `horizon` is a categorical forecast group and should not be interpreted as the numeric offset from `ts_index`
  - `weight` is used in the loss and must not be used as a feature
- Distinct values:
  - `code`: `23` in train and test
  - `sub_code`: `180` in train, `47` in test
  - `sub_category`: `5` in train and test
  - `(code, sub_code, sub_category)` groups: `9,270` in train, `2,784` in test
  - `horizon`: `[1, 3, 10, 25]`
- Time ranges:
  - train `ts_index`: `1..3601`
  - test `ts_index`: `3602..4376`
- Key integrity and overlap audit:
  - both train and test have zero duplicate `id` values,
  - both train and test have zero duplicate `(code, sub_code, sub_category, horizon, ts_index)` row keys,
  - both train and test have zero `id` reconstruction mismatches against `code__sub_code__sub_category__horizon__ts_index`,
  - test rows on unseen `(code, sub_code)` pairs: `1,289,694 / 1,447,107` (`89.12%`),
  - test rows on unseen `(code, sub_code, sub_category)` triplets: `1,289,694 / 1,447,107` (`89.12%`),
  - test rows on unseen `(code, sub_code, sub_category, horizon)` groups: `1,289,694 / 1,447,107` (`89.12%`),
  - test rows on unseen `(code, sub_category, horizon)` groups: `0 / 1,447,107` (`0%`).
- Row counts by horizon:
  - train:
    - `1`: `1,394,653`
    - `3`: `1,385,816`
    - `10`: `1,337,236`
    - `25`: `1,219,709`
  - test:
    - `1`: `379,617`
    - `3`: `376,558`
    - `10`: `362,057`
    - `25`: `328,875`
- Missingness:
  - several engineered features contain nulls in both splits,
  - notably sparse columns include `feature_at`, `feature_w`, `feature_x`, `feature_y`, `feature_z`, `feature_by`, `feature_cd`, `feature_ce`, and `feature_cf`,
  - null handling must be part of baseline model design.

## Metric And Rule Notes

- Working competition metric:

```python
def _clip01(x: float) -> float:
    return float(np.minimum(np.maximum(x, 0.0), 1.0))

def weighted_rmse_score(y_target, y_pred, w) -> float:
    denom = np.sum(w * y_target ** 2)
    ratio = np.sum(w * (y_target - y_pred) ** 2) / denom
    clipped = _clip01(ratio)
    val = 1.0 - clipped
    return float(np.sqrt(val))
```

- Public leaderboard uses about `25%` of the test set; private uses the remaining `75%`.
- Canonical offline proxy score in this repo is now the exact metric on the concatenated validation rows across folds, not a row-weighted average of per-fold scores.
- Sequential rule:
  - prediction at time `t` may only use data available up to `t`,
  - preprocessing on test must also be sequential,
  - any use of future test information is a rules violation.
- Host clarification:
  - top public scores may be leakage-driven,
  - final winning code must be reproducible,
  - public ranking is not a reliable proxy for private ranking.
- Target-derived feature clarification:
  - using target and weights on the training set is allowed,
  - but it can inflate CV and public scores,
  - the host said some hidden test points are ignored to reduce this inflation,
  - exact ignored points are undisclosed.
- Leakage hotspot:
  - community discussion specifically flagged `feature_al` as potentially target-reconstructive,
  - any reliance on it should be accompanied by explicit leakage checks.

## Semantics Audit Takeaways

- Submission and scoring are row-level, not an aggregated per-group task.
- The current `id` format is validated directly from local data and can be treated as a strict join key.
- The test split has heavy cold-start behavior below the `(code, sub_category, horizon)` level.
- Any validation scheme that only holds out the last train window will overestimate approaches that depend on `sub_code` or finer group history.
- A defensible early baseline can still use `(code, sub_category, horizon)` because that level has full test coverage without looking ahead.

## External Review Incorporation (`2026-04-10 UTC`)

This repo now has two external planning inputs archived under:

- `pro_responses/1.md`
- `pro_responses/2.md`

Key conclusions adopted into the docs:

- Do not treat `0.8+` or `0.8718` as a credible offline planning target under the current exact proxy. The public number remains useful only as a direct-leaderboard reference.
- Keep `aggregate_overall_score` as the canonical selector. Add a colder stress-report and veto layer instead of replacing the main metric.
- The current four-fold proxy is directionally correct but still warmer than test at `(code, sub_code)`, so warm-heavy variants need extra stress checks before promotion.
- The highest-leverage safe branch remains the current stacked incumbent family, not a generic feature-model or deep-sequence rewrite.

Conflict resolutions versus the pre-existing docs:

- Reordered next actions: the external review prefers stress diagnostics first, then clipped or gated recent-delta work, then new `OSJL3A7Y` / `X9BZ68VQ` warm overlays.
- `RecentMeanDeltaRegressor` already works at `(code, sub_category, horizon)`, so the next implementation change is activation control and clipping, not adding `code` to the grouping key.
- Generic boosting sweeps, deep sequence models, `feature_al`, and posthoc all-fold blend tuning remain explicitly deprioritized until the current safe stack stalls.

Concrete next-session implementation order derived from the external review:

1. Add a machine-readable `stress` block to the evaluation summary with combined `fold_02_03` score, cold/warm splits, per-horizon aggregates, and long-horizon watchlist slices.
2. Extend `RecentMeanDeltaRegressor` with horizon-specific clipping and gating controls, then compare those variants against `smoothed_stable_horizon_blend_last_target_recent_delta_v1` before wrapping new warm overlays around the winner.
3. Expand exact warm overlays in narrow bundles for `OSJL3A7Y` and `X9BZ68VQ` on horizons `25` and `10`, promoting only bundles that survive both the canonical score and the colder stress diagnostics.
4. Only after those branches are exhausted should the next session revisit a short-horizon stable-drift branch for horizons `1` and `3`, or another incumbent-based residual pass.

Watchlist slices to preserve in the stress report:

- `OSJL3A7Y × {DPPUO5X2, PZ9S1Z4V, PHHHVYZI} × {10,25}`
- `X9BZ68VQ × {DPPUO5X2, PHHHVYZI} × {10}`
- `X9BZ68VQ × {DPPUO5X2, PHHHVYZI, PZ9S1Z4V, NQ58FVQM} × {25}`

## Additional External Review Incorporation (`2026-04-10 UTC`, `pro_responses/3.md` to `pro_responses/6.md`)

New external review docs were added under:

- `pro_responses/3.md`
- `pro_responses/4.md`
- `pro_responses/5.md`
- `pro_responses/6.md`

Corrections adopted from those reviews:

- Under the repo metric, higher score is better, so the earlier wording that the pentaslice `X9BZ68VQ / horizon=25` core and `OSJL3A7Y / horizon=25` long-core retries "improved" their watchlists was wrong.
- Those two `h=25` warm-overlay retries regressed both the canonical selector and their own watchlist scores; clipping on already-bad warm subsets masked part of the raw damage.
- For already-clipped warm subsets, raw `error_sum` / `denom_sum` deltas matter more than the clipped score alone when deciding whether a branch is neutral, helpful, or harmful.

Direction adopted from those reviews:

- Stop same-family long-horizon warm-overlay micro-tuning. Reopen that region only with a materially different mechanism.
- The next modeling family should be incumbent-based residual correction rather than another recent-delta or warm-overlay sweep.
- The highest-priority modeling test is one conservative strict-OOF, feature-conditioned residual correction on top of `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1`, starting on horizons `1` and `3`.
- Keep that first residual experiment narrow: Ridge only, `weight` excluded, `feature_al` banned, conservative shrinkage, and canonical `aggregate_overall_score` still the promotion gate.
- Only if that first incumbent-based residual branch shows a real canonical lift without colder-stress damage should the repo spend more exact-CV budget on a specialist longwatch residual or a conditional expert blend around `OSJL3A7Y` / `X9BZ68VQ` on horizons `10` and `25`.

Windows exact-CV engineering order adopted from the performance review:

1. Add a whole-frame fast path for static validation-time predictors so the current winner family does not loop batch-by-batch over `ts_index` when the result is mathematically identical.
2. Stream folds one at a time in `scripts/evaluate_baselines.py` instead of preloading all fold data at once.
3. Fuse scoring/stress assembly so each fold-baseline pair is aligned once and reused for overall plus stress summaries.
4. Only after those changes are verified should the repo consider fit-artifact caching, merge-to-reindex lookup swaps, or a one-read narrow preload mode for the winner family.

## Milestone Board

- `Milestone 0`: completed
- `Milestone 1`: completed
- `Milestone 2`: completed
- `Milestone 3`: completed
- `Milestone 4`: completed
- `Milestone 5`: pending
- `Milestone 6`: pending

## Scaffold Added

- Package modules:
  - `src/ts_forecasting/constants.py`
  - `src/ts_forecasting/metrics.py`
  - `src/ts_forecasting/io.py`
  - `src/ts_forecasting/sequential.py`
  - `src/ts_forecasting/baselines.py`
  - `src/ts_forecasting/residuals.py`
  - `src/ts_forecasting/semantics.py`
  - `src/ts_forecasting/validation.py`
- CLI scripts:
  - `scripts/profile_data.py`
  - `scripts/evaluate_baselines.py`
  - `scripts/predict_baseline.py`
  - `scripts/audit_semantics.py`
  - `scripts/analyze_validation_errors.py`
  - `scripts/evaluate_residual_models.py`
- Tests:
  - `tests/test_metrics.py`
  - `tests/test_baselines.py`
  - `tests/test_sequential.py`
  - `tests/test_semantics.py`
  - `tests/test_validation.py`
  - `tests/test_residuals.py`

Current code capabilities:

- exact weighted competition metric helper,
- exact numerator / denominator metric breakdown helper for canonical fold aggregation,
- DuckDB-backed parquet loading helpers,
- sequential inference pipeline that predicts in increasing `ts_index` order,
- frozen train-stat imputer for future feature models,
- leakage-safe weighted mean baselines with fallback hierarchy and hierarchical shrinkage,
- data profiling CLI,
- competition semantics audit CLI,
- deterministic expanding-window fold generation,
- cold-start annotation and coverage summaries aligned to the observed test structure,
- forward-holdout and multi-fold baseline evaluation CLI,
- validation error analysis CLI over saved fold predictions,
- residual-slice helper utilities, prefix-only residual scale helpers, and a residual model evaluation CLI that can emit strict-OOF normalized target artifacts,
- fixed-prefix pseudo-history residual correction predictor for gated weak slices,
- generic fixed-weight linear blending for leakage-safe baseline predictors,
- selectable baseline/model subsets inside the forward-CV evaluator so heavier branches do not slow the default suite,
- baseline submission-generation CLI for grouped, residual-corrected, and blended predictors.
- reusable exact-group warm-start last-target overlays with optional code filters,
- reusable recent-vs-full-history mean-delta corrections on stable groups.

## Decisions

- Use a durable markdown stack modeled on the OpenAI long-horizon Codex workflow.
- Add `AGENTS.md` so the next Codex session is routed into the task automatically.
- Frame the task as offline improvement and reproducibility work by default because the public deadline has already passed.
- Treat the local parquet files as the initial data source of truth.
- Assume a forward time split because `ts_index` is contiguous from train into test, but keep that assumption explicit until validated against the competition rules.
- Standardize on the repo-local `.venv` for future Codex sessions.
- Use `requirements.txt` as the default dependency install source.
- Treat the user-supplied metric and rule summary as the working spec for evaluation and leakage control.
- Ban `weight` from the feature matrix by default.
- Treat public leaderboard chasing as lower priority than robust sequential offline validation.
- Start with a simple, causal scaffold before adding heavier feature-model code.
- Use DuckDB for dataset-scale reads and lightweight aggregation.
- Keep the first baseline family deliberately simple and leakage-resistant, even if the score is weak.
- Treat row-level prediction and cold-start group coverage as resolved competition semantics, not open assumptions.
- Treat `(code, sub_category, horizon)` as the coarsest stable group level currently known to cover the full test split.
- Defer feature-heavy modeling until validation reflects both forward time order and cold-start exposure.
- Record the available hardware so future sessions can consider GPU-capable models without guessing.
- A completed milestone is not, by itself, a stopping condition.
- Future sessions should keep iterating autonomously until the score path is competitive, blocked, or clearly plateaued.
- Use four non-overlapping `360`-step expanding-window folds as the current default offline proxy because they reproduce `75%` to `78%` unseen `(code, sub_code)` validation rows while preserving full `(code, sub_category, horizon)` coverage.
- Treat `(code, sub_category, horizon)` as the safest currently validated model level for target aggregation because it matches full test coverage and avoids relying on unseen `sub_code` histories.
- Prefer all-history stable-group statistics over short recent windows for the current grouped baselines; the recent-window ablation regressed.
- Use smoothed group means rather than raw fine-group means when warm-start performance collapses, because shrinkage improves both warm and cold slices without using future data.
- Use failure analysis on saved fold predictions to guide the next model branch; the current best baseline is weakest on horizons `1` and `3`, and on sub-category slices led by `DPPUO5X2`.
- Treat the exact concatenated-fold metric as the only canonical selector for offline model ranking; fold-average scores are now diagnostic only.
- Build residual models on top of `smoothed_stable_group_mean_pw100`, not as raw feature-only models, because the grouped baseline is the only branch with consistent signal so far.
- Keep `weight` out of the feature matrix and keep `feature_al` excluded from the initial residual branch until explicit leakage checks are added.
- Use small, shrunk residual corrections first; broad unshrunk corrections are not justified while folds `02` and `03` still clip.
- Keep the raw tri-slice residual branch as the best non-baseline residual branch for now; the strict-OOF normalized replacement branch is cleaner but currently tops out at `0.1123268577`, below `0.1128626342`.
- Treat the new research notes as directional guidance, not as evidence; every proposed idea still needs to clear the exact four-fold forward proxy before code is expanded around it.
- Do not replace the stable grouped baseline with a direct smoothed full-group warm-start mean: the naive `(code, sub_code, sub_category, horizon)` translation clipped to `0.0` overall and `0.0` on the warm-start slice under the canonical proxy.
- Treat convex blending of complementary train-only baselines as a first-class modeling path here; `horizon_mean` is weak on its own but materially improves the clipped metric when blended with the smoothed stable-group baseline.
- Promote `smoothed_stable_horizon_blend` to the best implemented offline baseline because it improved the exact four-fold score from `0.1119478178` to `0.1370805421`, raised cold-start score to `0.1562849407`, and lifted warm-start score above zero to `0.0712384530`.
- Keep `smoothed_stable_horizon_blend` as the incumbent even after rebuilding the exploratory raw tri-slice branch, because the clean implementation of `ridge_triweak_h13_tw720_s022_q90 + horizon_mean` scored `0.1370652148`, slightly below the incumbent `0.1370805421`.
- Treat the saved posthoc `raw_best + horizon_mean` edge as unresolved until the discrepancy between the old artifact materialization (`0.1375279103`) and the rebuilt clean path (`0.1370652148`) is explained.
- Deprioritize `feature_al` and other tiny feature-only branches as primary modeling paths; honest four-fold forward checks showed no useful standalone signal and no tiny safe feature set that beat the zero benchmark.
- Treat horizons `10` and `25` as the current highest-leverage drift slices; a train-only recent-vs-full-history delta at `(code, sub_category, horizon)` materially improved both cold and warm aggregate scores on top of the warm last-target overlay.
- Promote `smoothed_stable_horizon_blend_last_target_rules` over the raw residual family because a tightly gated exact-group warm overlay lifted the exact four-fold score to `0.1386781180` without reopening feature-leakage risk.
- Promote `smoothed_stable_horizon_blend_last_target_recent_delta_v1` over the plain warm overlay because it improved the exact four-fold score again to `0.1439924990`, with aggregate cold-start score `0.1601147203` and aggregate warm-start score `0.0937945231`.
- Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1` to the current best implemented path because a narrow `K7Y1TTAH / horizon=25` warm overlay further improved the exact four-fold score to `0.1450306103` and raised aggregate warm-start score to `0.0991358430`.
- Keep `aggregate_overall_score` as the canonical selector and treat the `stress` block as a veto/reporting layer only; the exact real-data clipped/gated delta reruns did not beat `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1` on either the canonical score or the key stress slices.
- Deprioritize the first alpha `0.03` OSJL/X9 warm probes on horizon `10`; all four exact runs regressed versus `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1`, and the best of them still finished below the incumbent at `0.1450009351`.
- Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1` to the current best implemented path because targeted short-horizon recent-delta overlays on horizons `1` and `3` improved the exact four-fold score to `0.1450467635`, improved `fold_02_03`, and held that promotion under the secondary `triplet` stress view.
- Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1` to the current best implemented path because a conservative local `h=1/3` beta increase improved the exact four-fold score again to `0.1450488508`, improved `fold_02_03`, `horizon=1`, and `horizon=3`, and held the same winner under the secondary `triplet` stress view while leaving the long-horizon watchlists unchanged.
- Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1` to the current best implemented path because widening the short-horizon drift overlay to all five sub-categories improved the exact four-fold score to `0.1451607523`, improved `aggregate_cold_start_score`, improved `aggregate_warm_start_score`, improved `fold_02_03`, and raised horizons `1` and `3` while leaving the long-horizon watchlists unchanged.
- Reject the next `h=3` OSJL/X9 exact warm-overlay probes as a promotion path for now; even on the improved `beta_hi` parent they regressed the canonical score and mostly worsened `fold_02_03`, so the next overlay attempt should move back to a different slice family rather than widening those `h=3` bundles.
- Reject the pentaslice `X9BZ68VQ / horizon=25` core warm-overlay probes as a promotion path for now; under the repo's higher-is-better metric they regressed both the targeted `x9bz68vq_h25_core` watchlist and the canonical `aggregate_overall_score`, so widening same-family H25 overlays is not justified.
- Reject the pentaslice `OSJL3A7Y / horizon=25` long-core warm-overlay probes as a promotion path for now; under the repo's higher-is-better metric they regressed both the targeted `osjl3a7y_long_core` watchlist and the canonical `aggregate_overall_score`, which closes the current long-horizon warm-overlay micro-tuning branch.
- Prioritize exact Windows forward-CV runtime work ahead of any new exact sweep: whole-frame static prediction, fold streaming, then single-pass score/stress assembly.
- Treat an incumbent-based residual branch as the next modeling family, with the first exact-CV attempt focused on a strict-OOF, feature-conditioned Ridge correction on horizons `1` and `3` on top of the current winner.
- Do not spend more exact-CV budget on specialist longwatch residuals or conditional expert blends unless that first incumbent-based residual branch shows a real canonical lift without colder-stress regression.
- Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1` to the current best implemented path because a final pentaslice `h=3` beta-only ratchet improved the exact four-fold score to `0.1451839276`, improved `aggregate_cold_start_score`, improved `aggregate_warm_start_score`, improved `fold_02_03`, improved `horizon=3`, and held the same winner under the secondary `triplet` stress view while leaving long-horizon watchlists unchanged.

## Open Questions

- Is any authenticated Kaggle access available in the next session?
- Does the user still want submission packaging despite the deadline status?
- What stress-report veto thresholds should be considered material enough to block promotion when `aggregate_overall_score` improves only slightly?
- How should target-derived features, if any, be cross-fitted or constrained so they remain defensible under the host’s leakage warnings?
- Does one conservative incumbent-based strict-OOF residual correction on horizons `1` and `3` beat `0.1451839276` canonically without hurting `fold_02_03`, cold-start robustness, or the long-horizon watchlists?
- Does the exact Windows forward-CV runtime optimization path preserve fold predictions and `summary.json` exactly while making exact reruns cheap enough to justify one more residual-family experiment?
- If the first incumbent-based residual branch shows positive signal, is the next justified follow-up a specialist longwatch residual or a conditional expert blend on `OSJL3A7Y` / `X9BZ68VQ` horizons `10` and `25`, or is the safer move to stop at the first lift?
- Has unattended exact-CV improvement reached a real plateau around `0.14518` if the first incumbent-based residual experiment fails cleanly?
- Can a second-stage OOF-normalized correction on top of the current best raw tri-slice branch beat `0.1128626342`, or is a different representation needed?
- Why does the rebuilt clean `ridge_triweak_h13_tw720_s022_q90` path underperform the saved raw artifact materialization by about `0.00078` absolute score, despite matching the same broad config and slice definition?

## Blockers

- No authenticated Kaggle session is available in this setup step.
- Public submission access is still unconfirmed and should be treated as unavailable until explicitly verified.
- The exact subset ignored by the host’s rescoring logic is hidden, so offline validation can only approximate final scoring.
- The new best exact offline score is `0.1451839276`, which is a real gain of `+0.0000231753` over the prior incumbent, but the long-horizon OSJL/X9 watchlists remain the main unresolved weakness and now appear resistant to the tested leakage-safe warm overlays; after the final successful `h=3` beta-only revisit, unattended progress looks close enough to plateau that further micro-tuning should be deprioritized until a materially different idea or live submission feedback becomes available.
- A sampled feature-only `HistGradientBoostingRegressor` probe did not beat the zero benchmark, so feature modeling still needs better failure analysis before more code is committed.
- The new best implemented blend improves the exact concatenated-fold score materially versus earlier baselines, but there is still a large gap to the public reference score path.
- The strict-OOF normalized replacement branch plateaued at `0.1123268577` after shrink sweeps and narrow-slice probes, so it is not yet a justified replacement for the incumbent raw tri-slice branch.
- The rebuilt fixed-prefix raw tri-slice branch is now reproducible and testable, but its clean implementation did not match the older saved artifact score, so the tiny apparent upside of that stack is not yet trustworthy.

## Suggested First Actions For The Next Session

1. Treat the short-horizon `h=1/3` all-sub-category overlay as the current best-known closure of that branch; do not spend more exact-CV budget there unless a new slice hypothesis is clearly different from the now-exhausted five-sub-category overlay.
2. If work continues, first optimize the exact Windows forward-CV path in the low-risk order: whole-frame static prediction, fold streaming, then single-pass score/stress assembly, and verify exact summary equality after each step.
3. Treat the pentaslice `h=3` beta-only ratchet as the last justified same-family exact-CV micro-tuning step that still paid off; keep the long-horizon warm-overlay branch closed and assume further overlay knob sweeps are low value until a genuinely different mechanism appears.
4. Materialize or reuse an exact base summary for `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1`, then run one conservative incumbent-based strict-OOF residual experiment on horizons `1` and `3` only, with Ridge, strong shrinkage, `feature_al` banned, and `weight` excluded.
5. Promote that residual family only if it beats `0.1451839276` on `aggregate_overall_score` and also improves at least one of `fold_02_03`, `horizon=1`, or `horizon=3` without materially damaging colder slices or long-horizon watchlists.
6. Only if Step 5 is positive should the next exact-CV branch be a specialist longwatch residual or a conditional expert blend for `OSJL3A7Y` / `X9BZ68VQ` on horizons `10` and `25`; otherwise treat the repo as near a true unattended plateau.
7. If authenticated submission access returns, package and submit `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1` first, then compare leaderboard behavior against the exact proxy before branching further.
8. Confirm the competition state and whether authenticated Kaggle access is available.

## Autonomous Stop Rule

The session should not stop merely because a milestone or single experiment completed.

Preferred stopping conditions:

- verified public score is close to or above `0.8718`,
- strongest offline validation has plateaued after several materially different experiment branches,
- a hard external blocker requires user intervention,
- leakage risk cannot be resolved safely.

Do not interpret `0.8718` as an offline score expectation while using the current exact proxy.

## Experiment Log

Use this format for every experiment:

| ID | Milestone | Hypothesis | Command / Config | Result | Decision |
| --- | --- | --- | --- | --- | --- |
| E000 | Setup | Created durable markdown task stack for future session handoff | `n/a` | Ready for execution | Start with repo bootstrap |
| E001 | Setup | Local data can replace the initial missing-data assumption | repo-local DuckDB profile over `data/train.parquet` and `data/test.parquet` | Confirmed train/test schema, forward `ts_index` split, train-only `y_target` and `weight` | Update docs and start with local profiling code in the next run |
| E002 | Setup | Standardizing the Python environment now will reduce session drift later | `. .venv/bin/activate && python -m pip install -r requirements.txt` | Installed baseline data and test stack into repo-local venv | Future sessions should activate `.venv` first |
| E003 | Setup | User-provided rules materially narrow the competition ambiguity | Incorporated dataset description, exact metric, sequential rule, and leakage discussion into long-run docs | Reduced ambiguity around scoring and allowed/forbidden processing | Use these notes as the working competition spec |
| E004 | Milestone 0 | A small reusable code scaffold will reduce drift in future sessions | Added `src/`, `scripts/`, and `tests/` with metric, IO, causal inference, and baseline helpers | Runnable project surface now exists | Treat the bootstrap phase as complete |
| E005 | Milestone 0 | The scaffold should validate cleanly before more modeling work | `. .venv/bin/activate && pytest -q -s tests` | `5 passed in 0.29s` | Keep tests green as the codebase grows |
| E006 | Milestone 1 | A dedicated profiling CLI should reproduce the known dataset facts from local parquet files | `. .venv/bin/activate && python scripts/profile_data.py --output-dir outputs/profiles` | Wrote profile artifacts successfully | Use these outputs as local schema references |
| E007 | Milestone 2 | Very simple causal baselines can establish a first honest lower bound | `. .venv/bin/activate && python scripts/evaluate_baselines.py --train-path data/train.parquet --holdout-steps 30 --output-dir outputs/baselines` | `horizon_mean=0.0283453`, `hierarchical_group_mean=0.0` on holdout `ts_index > 3571` | The horizon-only baseline is weak but stable; naive grouped memorization is not robust |
| E008 | Milestone 2 | The baseline submission path should run end to end on the real test file | `. .venv/bin/activate && python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline horizon_mean --output outputs/submissions/horizon_mean_submission.csv` | Wrote submission CSV successfully | Submission generation path works for future experiments |
| E009 | Milestone 1 | A dedicated semantics audit should resolve the prediction unit and quantify train/test overlap before more modeling work | `. .venv/bin/activate && python scripts/audit_semantics.py --train-path data/train.parquet --test-path data/test.parquet --output-dir outputs/semantics` | Confirmed row-level prediction keys; `89.12%` of test rows are on unseen `(code, sub_code)` pairs and also unseen triplet/full-group keys, while `(code, sub_category, horizon)` still covers the entire test set | Mark Milestone 1 complete and prioritize a cold-start-aware validation design |
| E010 | Milestone 1 | The semantics audit and existing scaffold should remain validated after adding the new audit code | `. .venv/bin/activate && pytest -q -s tests/test_semantics.py tests/test_metrics.py tests/test_baselines.py tests/test_sequential.py` | `6 passed in 1.46s` | Keep the semantics audit in the default smoke suite |
| E011 | Setup | Future sessions should know the available hardware up front | Recorded GPU, RAM, and CPU in the durable task files | Reduced ambiguity around when GPU-capable methods are worth trying | Consider GPU use in later model milestones, not by default in every step |
| E012 | Milestone 3 | A deterministic multi-fold validation scheme should match the real test cold-start structure better than a single tail holdout | Added `src/ts_forecasting/validation.py`, extended `scripts/evaluate_baselines.py`, and added `tests/test_validation.py`; `. .venv/bin/activate && pytest -q -s tests` | `10 passed in 1.10s` after the new validation helpers were wired in | Mark Milestone 3 implementation complete and use the new fold loop as the default offline proxy |
| E013 | Milestone 3 | Four non-overlapping `360`-step expanding folds can approximate the test cold-start rate while preserving stable-group coverage | `. .venv/bin/activate && python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --output-dir outputs/baselines` | Validation folds showed `75.04%` to `77.98%` unseen `(code, sub_code)` rows vs real-test `89.12%`, while `(code, sub_category, horizon)` stayed fully covered in every fold | Treat this fold scheme as the current best offline proxy and evaluate models against it |
| E014 | Milestone 4 | A sampled feature-only tree model might beat the weak grouped baselines on the newest long fold | Ad hoc `.venv` `HistGradientBoostingRegressor` probe on fold `3241 -> 3601` using `200k` sampled recent rows, `(code, sub_category, horizon)`, and a 26-feature subset | Scored `0.0` with ratio `1.0014`, so it failed to beat the zero benchmark | Do not formalize raw feature-only modeling until failure analysis explains why it is underperforming |
| E015 | Milestone 4 | A baseline restricted to the stable coverage level `(code, sub_category, horizon)` should outperform horizon-only means under cold-start-aware CV | Re-ran `scripts/evaluate_baselines.py` with a new `stable_group_mean` config on the four-fold scheme | `stable_group_mean` reached row-weighted score `0.07760` vs `0.00569` for `horizon_mean`; the full hierarchical baseline stayed at `0.0` overall | Promote stable-group aggregation as the new causal reference model and demote full-group memorization |
| E016 | Milestone 4 | Recent-window grouped averages might handle drift better than all-history grouped averages | Ad hoc window ablation over stable-group means with windows `720`, `1440`, and full history across the four long folds | Mean scores were `0.02391` for `720`, `0.06590` for `1440`, and `0.07780` for full history | Keep all-history grouped statistics for now; shorter windows are not the fix |
| E017 | Milestone 4 | Shrinking stable-group means toward coarser priors should reduce overfitting on noisy groups | Added `SmoothedWeightedGroupMeanRegressor` plus tests, then re-ran `. .venv/bin/activate && python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --output-dir outputs/baselines` | `smoothed_stable_group_mean_pw100` became best known with row-weighted score `0.09817`, mean cold-start score `0.12998`, and mean warm-start score `0.06733` | Mark Milestone 4 complete at the grouped-model level and shift the next loop to failure analysis plus residual/hybrid improvements |
| E018 | Milestone 6 | The current best-known grouped baseline should be runnable end to end on the real test file | `. .venv/bin/activate && python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_group_mean_pw100 --output outputs/submissions/smoothed_stable_group_mean_pw100_submission.csv` | Wrote the full submission-format CSV successfully | Use this artifact as the current reproducible inference path while better models are developed |
| E019 | Milestone 5 | Saved fold predictions should reveal which slices still cause the strongest baseline to fail | `. .venv/bin/activate && python scripts/analyze_validation_errors.py --summary-path outputs/baselines/forward_cv_h360_f4_s360_code_sub_code/summary.json --train-path data/train.parquet --baseline smoothed_stable_group_mean_pw100 --output-dir outputs/analysis --top-k 3` | Aggregate score on concatenated fold rows was `0.11195`; horizons `1` and `3` were the weakest, and sub-category `DPPUO5X2` was the worst persistent slice, with folds `02` and `03` also collapsing on `PZ9S1Z4V` and `PHHHVYZI` across multiple horizons | Use the next model loop to attack these slices specifically instead of broad untargeted feature experiments |
| E020 | Milestone 5 | The offline loop must rank models by the same exact aggregate metric used in downstream diagnostics | Updated `src/ts_forecasting/metrics.py`, `scripts/evaluate_baselines.py`, and tests, then re-ran `. .venv/bin/activate && python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --output-dir outputs/baselines` | `summary.json` now uses canonical `aggregate_overall_score`, and the best baseline is `smoothed_stable_group_mean_pw100` at exact score `0.1119478178` rather than the stale row-weighted `0.09817` selector | Treat the exact concatenated-fold score as the only canonical offline objective from here forward |
| E021 | Milestone 5 | Stronger diagnostics should explain whether zero-scored folds are true failures or denominator pathologies | Extended `scripts/analyze_validation_errors.py` and re-ran `. .venv/bin/activate && python scripts/analyze_validation_errors.py --summary-path outputs/baselines/forward_cv_h360_f4_s360_code_sub_code/summary.json --train-path data/train.parquet --baseline smoothed_stable_group_mean_pw100 --output-dir outputs/analysis --top-k 10` | Added `code / sub_category / horizon` slices, fold `02` vs `03` comparisons, warm vs cold splits inside each slice, and per-slice energy denominators; aggregate weak slices remain concentrated in short horizons and in `DPPUO5X2`, `PZ9S1Z4V`, and `PHHHVYZI` | Use these tables to target the next residual experiments and to distinguish real numerator failures from small-denominator noise |
| E022 | Milestone 5 | A residual model on top of the smoothed stable-group baseline should beat the base model before any leakage-risky feature expansion | Added `src/ts_forecasting/residuals.py`, `tests/test_residuals.py`, and `scripts/evaluate_residual_models.py`; ran `. .venv/bin/activate && python scripts/evaluate_residual_models.py --base-summary-path outputs/baselines/forward_cv_h360_f4_s360_code_sub_code/summary.json --train-path data/train.parquet --base-baseline smoothed_stable_group_mean_pw100 --output-dir outputs/residual_models` | DPP-only residuals gave small gains: best exact score was `0.1122213767` from `ridge_dppuo5x2_h13_tw720_s020_q90`; `Huber` improved only slightly and shallow `HistGradientBoostingRegressor` regressed | Keep the residual-on-baseline direction, but broaden the weak slice set before considering heavier models |
| E023 | Milestone 5 | Adding `PZ9S1Z4V` and `PHHHVYZI` to the short-horizon residual slice should outperform the DPP-only residual branch | Expanded `scripts/evaluate_residual_models.py` with tri-slice Ridge/Huber configs over horizons `1` and `3`, excluding `feature_al` and shrinking corrections toward zero | `ridge_triweak_h13_tw720_s010_q90` improved the exact score to `0.1125969381`, with numerator reductions in folds `01`, `02`, `03`, and `04` even though folds `02` and `03` still clipped to `0.0` | Promote the tri-slice Ridge residual family as the new best branch and tune shrinkage before widening feature usage |
| E024 | Milestone 5 | The best tri-slice residual family may need a larger global shrinkage multiplier than the first conservative setting | Posthoc exact rescaling of saved `ridge_triweak_h13_tw720_s010_q90` corrections against the base predictions, then materialized `ridge_triweak_h13_tw720_s022_q90` predictions and summary rows under `outputs/residual_models` | Best exact offline score improved again to `0.1128626342`, a gain of `+0.0009148164` over the canonical smoothed baseline, with the strongest numerator reduction in fold `02` and smaller positive gains in folds `01` and `04` | Current best branch is now the shrunk tri-slice Ridge residual; next work should focus on better residual targets and leakage-safe feature expansion rather than more blind scale sweeps |
| E025 | Milestone 5 | A strict-OOF residual target with prefix-only group normalization may be cleaner and stronger than the current fixed-prefix pseudo-history | Extended `src/ts_forecasting/residuals.py`, rewrote `scripts/evaluate_residual_models.py`, and added `tests/test_residuals.py` coverage for scale lookup and normalized correction handling; validated with `. .venv/bin/activate && PYTHONPATH=src pytest -q -s tests/test_residuals.py tests/test_metrics.py tests/test_validation.py` and `. .venv/bin/activate && PYTHONPATH=src python -m py_compile scripts/evaluate_residual_models.py` | The helper layer passed cleanly and the residual CLI now writes `oof_base_predictions.parquet` and `oof_residual_pool.parquet` under `outputs/residual_models/forward_cv_h360_f4_s360_code_sub_code_oof_norm` | Use the new OOF artifacts as the clean target-construction path for all follow-up probes on this branch |
| E026 | Milestone 5 | Horizon-specific OOF-normalized tri-slice residual models may improve folds `02` and `03` more safely than the incumbent raw branch | `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_residual_models.py --base-summary-path outputs/baselines/forward_cv_h360_f4_s360_code_sub_code/summary.json --train-path data/train.parquet --base-baseline smoothed_stable_group_mean_pw100 --output-dir outputs/residual_models` | Untuned strict-OOF normalized configs regressed sharply; the best direct run was only `0.1101621570` from `ridge_oofnorm_triweak_h1_tw720_s015_q85`, while folds `02` and `03` stayed clipped and fold `04` degraded materially | Do not trust unshrunk OOF-normalized corrections; inspect shrink sensitivity before promoting or discarding the branch |
| E027 | Milestone 5 | The OOF-normalized branch may still recover if its corrections are shrunk far more aggressively than the first direct run used | One-off posthoc exact rescale sweep over the saved `outputs/residual_models/forward_cv_h360_f4_s360_code_sub_code_oof_norm` fold predictions, then materialized `ridge_oofnorm_triweak_h13_tw720_s003_q85_posthoc` plus `posthoc_rescale_sweep.json` under the same directory | The best tuned OOF-normalized score was `0.1123268577` from `ridge_oofnorm_triweak_h13_tw720_s015_q85` rescaled to `0.03`; fold `02` error sum fell to `47,497,660.60` and fold `03` to `48,600,062.22`, but the branch still trailed the incumbent `0.1128626342` | Mark the strict-OOF normalized replacement path as cleaner but currently weaker than the incumbent raw tri-slice branch |
| E028 | Milestone 5 | If OOF normalization is sound but the broad tri-slice fit is too diffuse, scale-aware weighting or a narrower `DPPUO5X2` slice might recover the gap | Ad hoc probes on the saved OOF parquet artifacts: one used training weights proportional to `weight * residual_scale^2`, and another trained DPP-only short-horizon Ridge variants with posthoc shrink sweeps | Neither rescue path beat the tuned OOF-normalized tri-slice result; the scale-aware weighting probe peaked at `0.1120444512`, and the best DPP-only probe stayed at `0.1120033400` | Treat this replacement branch as plateaued for now and move the next effort to a stack or materially different representation instead of more micro-tuning |
| E029 | Milestone 5 | The newly added research may justify a simple warm-start baseline branch if a direct smoothed fine-group translation survives the canonical forward proxy | Ad hoc `.venv` probe using `SmoothedWeightedGroupMeanRegressor` with fallback levels `[(code, sub_code, sub_category, horizon), (code, sub_category, horizon), (sub_category, horizon), (horizon)]`, `prior_weight=100.0`, and the exact four-fold forward proxy with `code_sub_code` cold-start annotation | The direct warm-start translation failed completely: every fold scored `0.0`, aggregate overall score was `0.0`, aggregate cold-start score stayed `0.1451415055`, and aggregate warm-start score was `0.0` | Reject naive fine-group replacement means; only pursue research-inspired warm-start ideas as additive, heavily gated overlays or as stacked experts on top of the stable parent baseline |
| E030 | Milestone 5 | The saved fold artifacts may already contain a stronger leakage-safe stack than the incumbent raw tri-slice residual branch | Exact quadratic posthoc search over saved OOF prediction artifacts from `horizon_mean`, `smoothed_stable_group_mean_pw100`, `ridge_triweak_h13_tw720_s022_q90`, and the best OOF-normalized branch; plus forward-calibrated scalar sanity checks on older folds only | The strongest implemented-safe candidate was the baseline-only `smoothed_stable_group_mean_pw100 + horizon_mean` blend at exact score `0.1370805421`; an exploratory `raw_best + horizon_mean` pair reached `0.1375279103` posthoc and `0.1209444254` under forward-calibrated scalar updates | Formalize the baseline-only blend first because it has a clean test-time inference path, then revisit the residual-plus-horizon blend as a follow-up |
| E031 | Milestone 5 | A fixed linear blend of `smoothed_stable_group_mean_pw100` and `horizon_mean` can turn the weak horizon baseline into a useful risk-reducing correction without adding leakage-sensitive features | Added `LinearBlendRegressor`, exposed `smoothed_stable_horizon_blend` in the baseline CLIs with weights `[0.6355710510, 0.3644289490]`, then ran `. .venv/bin/activate && PYTHONPATH=src pytest -q -s tests`, `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --output-dir outputs/baselines`, and `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend --output outputs/submissions/smoothed_stable_horizon_blend_submission.csv` | The new blend became the best implemented offline path with exact score `0.1370805421`, cold-start score `0.1562849407`, warm-start score `0.0712384530`, and a full submission-format test prediction file | Promote `smoothed_stable_horizon_blend` to the current best-known implemented pipeline and use the new submission artifact as the default reproducible inference path |
| E032 | Milestone 5 | Rebuilding the raw tri-slice residual branch as a reusable fixed-prefix predictor might reproduce the saved artifact edge and make the `raw + horizon` stack promotion-worthy | Added `FixedPrefixResidualCorrectionRegressor`, exposed `ridge_triweak_h13_tw720_s022_q90` plus `ridge_triweak_h13_tw720_s022_q90_horizon_blend` in the baseline CLIs, validated with `. .venv/bin/activate && PYTHONPATH=src pytest -q -s tests`, ran `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend ridge_triweak_h13_tw720_s022_q90 ridge_triweak_h13_tw720_s022_q90_horizon_blend --output-dir outputs/advanced_models`, and ran `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline ridge_triweak_h13_tw720_s022_q90_horizon_blend --output outputs/submissions/ridge_triweak_h13_tw720_s022_q90_horizon_blend_submission.csv` | The clean raw branch scored `0.1120846714`, and its formalized horizon blend scored `0.1370652148`, which is reproducible and end-to-end valid but still `0.0000153273` below the incumbent `smoothed_stable_horizon_blend`; the new test prediction CSV was written successfully and the smoke suite finished at `20 passed in 1.86s` | Keep the rebuilt branch available for follow-up analysis, but do not promote it over the incumbent until the artifact discrepancy is explained |
| E033 | Milestone 5 | A tightly gated exact-group warm overlay may recover warm-start drift without destabilizing cold rows | Added `WarmStartLastTargetBlendRegressor`, exposed `smoothed_stable_horizon_blend_last_target_rules`, validated with `. .venv/bin/activate && PYTHONPATH=src pytest -q -s tests`, ran `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend smoothed_stable_horizon_blend_last_target_rules --output-dir outputs/advanced_models`, and ran `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_rules --output outputs/submissions/smoothed_stable_horizon_blend_last_target_rules_submission.csv` | The exact warm overlay improved the canonical four-fold score from `0.1370805421` to `0.1386781180`, mainly by lifting warm rows while leaving cold-start behavior unchanged; the full submission-format test prediction file was written successfully | Promote the warm last-target overlay over the raw residual family and keep using tightly gated additive warm-start ideas rather than direct fine-group replacements |
| E034 | Milestone 5 | A stable-group recent-vs-full-history delta on horizons `10` and `25` may correct the remaining drift on top of the warm last-target overlay | Added `RecentMeanDeltaRegressor`, exposed `smoothed_stable_horizon_blend_last_target_recent_delta_v1`, validated with `. .venv/bin/activate && PYTHONPATH=src pytest -q -s tests/test_baselines.py tests/test_residuals.py`, ran `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_rules smoothed_stable_horizon_blend_last_target_recent_delta_v1 --output-dir outputs/advanced_models`, and ran `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_recent_delta_v1 --output outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_v1_submission.csv` | The recent-delta overlay improved the canonical four-fold score again to `0.1439924990`, with aggregate cold-start score `0.1601147203` and aggregate warm-start score `0.0937945231`; the full submission-format test prediction file was written successfully and the full suite reached `23 passed in 2.07s` before the next edit round | Promote the recent-delta branch as the new mainline incumbent and focus follow-up work on narrow code-specific overlays or clipped delta refinements |
| E035 | Milestone 5 | A narrow code-specific warm overlay may still add on top of the recent-delta incumbent if it only touches the strongest recurring miss slice | Generalized `WarmStartLastTargetBlendRegressor` to support optional code filters, exposed `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1`, validated with `. .venv/bin/activate && PYTHONPATH=src pytest -q -s tests`, ran `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output-dir outputs/stacked_models`, and ran `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1_submission.csv` | The `K7Y1TTAH / horizon=25` warm overlay improved the canonical four-fold score further to `0.1450306103`, raising aggregate warm-start score to `0.0991358430`; the full suite finished at `24 passed in 2.34s` and the full submission-format test prediction file was written successfully | Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1` to the current best implemented path and extend the same narrow-overlay pattern to other recurring miss codes only if they clear the exact proxy |
| E036 | Milestone 5 | External GPT-5.4 Pro recommendations should be turned into repo-level operating docs before the next implementation pass | Read `pro_responses/1.md` and `pro_responses/2.md`; updated `prompt.md`, `plans.md`, `documentation.md`, and `implement.md` with the revised selector policy, next-run priority order, and implementation watchlists | Recorded that `aggregate_overall_score` remains canonical, the next branch order is `stress diagnostics -> clipped or gated recent-delta -> OSJL/X9 warm overlays`, and `0.8718` should be treated as a public reference rather than a credible offline target under the current proxy; no code paths changed in this doc-only pass | Use the updated docs as the bootstrap for the next implementation session |
| E037 | Milestone 5 | A Windows-native checkout will be easier to continue from than the unstable WSL network state, but the handoff must preserve the exact resume point | Created private GitHub repo `meettilavat/Time_series_forecasting`, committed and pushed `main` at `6e062f8`, validated with `. .venv/bin/activate && pytest -q -s tests`, and updated `documentation.md` with the pushed location plus the interrupted clipped/gated recent-delta status | The repo is now resumable from GitHub, the test suite passed (`24 passed in 2.72s`), and the docs explicitly note that `RecentMeanDeltaRegressor` contains partial clipped/gated scaffolding that still needs registry wiring, tests, and stress-summary integration | Resume on Windows-native by implementing the `stress` block first, then finish the clipped/gated delta branch before touching OSJL/X9 overlays |
| E038 | Milestone 5 | The interrupted stress/clipped-delta WIP can be finished safely by centralizing the baseline registry, wiring the missing knobs in both CLIs, and validating the new summary structure before any new scoring run | Created Windows `.venv` with `python -m venv .venv` and `.\\.venv\\Scripts\\python.exe -m pip install -r requirements.txt`; added `src/ts_forecasting/baseline_registry.py`; updated `scripts/evaluate_baselines.py`, `scripts/predict_baseline.py`, `src/ts_forecasting/baselines.py`, and tests; ran `.\\.venv\\Scripts\\python.exe -m pytest -q -s tests/test_evaluate_baselines.py tests/test_baselines.py`, `.\\.venv\\Scripts\\python.exe -m pytest -q -s tests`; attempted `.\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_hbeta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_osjlx9_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output-dir outputs/advanced_models`, which failed because `data/train.parquet` is missing in this checkout; then created synthetic parquet smoke files under `outputs/smoke_data` and ran `scripts/evaluate_baselines.py` plus `scripts/predict_baseline.py` against them | Focused RED/GREEN coverage finished at `11 passed in 1.42s`, the full suite finished at `28 passed in 1.65s`, the smoke evaluation summary now emits non-null `stress.fold_sets.fold_02_03`, `per_horizon`, and watchlist aggregates, and `predict_baseline.py` wrote `outputs/smoke_predictions/recent_delta_clip_hbeta_submission.csv`; the real four-fold ranking did not run because the repo currently lacks `data/train.parquet` and `data/test.parquet` | Keep `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1` as the incumbent, restore the real parquet files, rerun the exact clipped/gated delta sweep under the new stress block, and only then proceed to OSJL/X9 warm-overlay experiments |
| E039 | Milestone 5 | The clipped/gated recent-delta family must be ranked on the real parquet files with the new `stress` block before any new overlay branch is promoted | Ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_hbeta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_osjlx9_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output-dir outputs/advanced_models/delta_sweep_exact_20260410_code_sub_code`, then reran the short list with `--cold-start-level triplet` into `outputs/advanced_models/delta_sweep_triplet_20260410_shortlist`, and wrote a fresh parent submission with `& .\\.venv\\Scripts\\python.exe scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1_submission.csv` | The real parquet files are present in this checkout, `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1` remained the stress-vetted delta winner at `0.1450306103`, and all clipped/gated alternatives finished lower (`0.1439924990`, `0.1428907137`, `0.1424447814`, `0.1395588259`); the secondary triplet view did not overturn that ordering | Keep `smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1` as the canonical delta parent and continue to the next branch only after treating the `stress` block as a veto/report rather than a replacement selector |
| E040 | Milestone 5 | Small exact warm overlays on the highest-watchlist OSJL/X9 slices may still add on top of the stress-vetted `k7` parent if they stay narrow enough | Added four alpha `0.03` warm-probe configs in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests`, then ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_osjl_dppu10_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_osjl_ph10_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_osjl_pz10_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_x9_ph10_a003_v1 --output-dir outputs/advanced_models/osjl_x9_warm_probe_exact_20260410` | The code path validated cleanly (`29 passed in 1.78s`), but every probe regressed versus the incumbent parent: `0.1449966298`, `0.1449821908`, `0.1450009351`, and `0.1449949945` versus `0.1450306103`; the best probe still weakened the exact proxy slightly | Park the first OSJL/X9 alpha `0.03` warm-probe branch and do not promote or widen it unless a materially different parent predictor changes the slice economics |
| E041 | Milestone 5 | A second short-horizon recent-delta layer on horizons `1` and `3` may improve the `k7` incumbent without giving back the stronger long-horizon behavior | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_dppu_v1` and `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1` in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests`, ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_dppu_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1 --output-dir outputs/advanced_models/h13_drift_exact_20260410`, reran the winner under `--cold-start-level triplet` into `outputs/advanced_models/h13_drift_triplet_20260410`, and wrote `outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1_submission.csv` | The full suite stayed green (`30 passed in 1.69s`), `..._k7_h13_dppu_v1` reached `0.1450458973`, and `..._k7_h13_trislice_v1` became the new best exact offline model at `0.1450467635`; it improved `fold_02_03` to `0.0861320784`, raised `horizon=1` to `0.0334336351` and `horizon=3` to `0.0571347947`, left horizons `10` and `25` unchanged, and held the same promotion under the secondary triplet view | Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1` as the new canonical incumbent and continue, if needed, with only a conservative local sweep around this short-horizon neighborhood |
| E042 | Milestone 5 | A very small local `h=1/3` parameter sweep may still produce a stress-safe gain on top of `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1` | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_lo_v1`, `..._beta_hi_v1`, and `..._rw360_v1` in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`31 passed in 1.81s`), ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_lo_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_rw360_v1 --output-dir outputs/advanced_models/h13_followup_exact_20260410`, reran the winner under `--cold-start-level triplet` into `outputs/advanced_models/h13_followup_triplet_20260410`, and wrote `outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1_submission.csv` | `..._beta_hi_v1` became the new best exact offline model at `0.1450488508`, ahead of `..._k7_h13_trislice_v1` by `0.0000020873`; it improved `fold_02_03` to `0.0861851770`, improved `horizon=1` to `0.0334554149`, improved `horizon=3` to `0.0571710131`, held the long-horizon watchlists unchanged, and kept the same winner under the secondary triplet view | Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1` as the new canonical incumbent, but treat this local neighborhood as near-plateaued because the gain was only in the low single-digit micro range |
| E043 | Milestone 5 | The current incumbent still has large exact warm-startable `h=3` failures for `OSJL3A7Y` and `X9BZ68VQ`, so narrow code-specific exact warm overlays might add without changing cold rows | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_osjl_h3_core_a003_v1`, `..._osjl_h3_core_a005_v1`, `..._x9_h3_core_a003_v1`, and `..._x9_h3_core_a005_v1` in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`32 passed in 1.84s`), then ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_osjl_h3_core_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_osjl_h3_core_a005_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_x9_h3_core_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_x9_h3_core_a005_v1 --output-dir outputs/advanced_models/h3_osjl_x9_warm_probe_exact_20260410` | The incumbent held at `0.1450488508`; the best new probe was `..._osjl_h3_core_a005_v1` at `0.1450148486`, while the X9 variants were lower still. The `h=3` overlays slightly helped fold `01` warm rows but regressed the canonical score and did not survive `fold_02_03` scrutiny | Park the `h=3` OSJL/X9 exact warm-overlay branch and, if more overlay work is justified, move back to a different slice family rather than widening these bundles |
| E044 | Milestone 5 | The short-horizon drift branch may still have real headroom if the overlay is widened from the tri-slice to all five sub-categories on horizons `1` and `3` | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_v1` and `..._pentaslice_beta_hi_v1` in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`33 passed in 1.73s`), ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 --output-dir outputs/advanced_models/h13_pentaslice_exact_20260410`, reran the winner under `--cold-start-level triplet` into `outputs/advanced_models/h13_pentaslice_triplet_20260410`, and wrote `outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1_submission.csv` | `..._pentaslice_beta_hi_v1` became the new best exact offline model at `0.1451607523`, a real gain of `+0.0001119016` over the prior incumbent; it improved `aggregate_cold_start_score` to `0.1602106456`, improved `aggregate_warm_start_score` to `0.0994123630`, improved `fold_02_03` to `0.0864672036`, improved `horizon=1` to `0.0340534189`, improved `horizon=3` to `0.0592252293`, left horizons `10` and `25` unchanged, and held the same promotion under the secondary triplet view | Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1` as the new canonical incumbent and shift the next search to a materially different long-horizon or code-specific branch because the short-horizon sub-category dimension is now largely exhausted |
| E045 | Milestone 5 | A narrow `NQ58FVQM / horizon=25` exact warm overlay on the older tri-slice beta-high parent might improve the unchanged long-horizon weak slices without affecting cold rows | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_osjl_h25_nq_a003_v1`, `..._a005_v1`, `..._x9_h25_nq_a003_v1`, and `..._x9_h25_nq_a005_v1` in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`34 passed in 1.70s`), then ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_osjl_h25_nq_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_osjl_h25_nq_a005_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_x9_h25_nq_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_x9_h25_nq_a005_v1 --output-dir outputs/advanced_models/h25_nq_warm_probe_exact_20260410` | The older tri-slice incumbent still held at `0.1450488508`; every `NQ58FVQM / horizon=25` probe regressed overall and weakened `horizon=25`, confirming that an NQ-only retry is not a justified promotion path, especially now that the canonical parent is the stronger pentaslice branch | Park the NQ-only long-horizon branch and, if long-horizon overlay work continues, move to the full `X9BZ68VQ / horizon=25` core bundle on top of the pentaslice incumbent rather than another NQ-only retry |
| E046 | Milestone 5 | The full `X9BZ68VQ / horizon=25` core bundle on top of the pentaslice incumbent might improve the unchanged long-horizon watchlist enough to justify promotion | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_x9_h25_core_a003_v1` and `..._a005_v1` in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`35 passed in 1.73s`), then ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_x9_h25_core_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_x9_h25_core_a005_v1 --output-dir outputs/advanced_models/x9_h25_core_warm_probe_exact_20260410` | Neither new probe beat the incumbent `0.1451607523`; `..._x9_h25_core_a003_v1` scored `0.1448311663` and `..._x9_h25_core_a005_v1` scored `0.1445990007`. The branch did improve its targeted stress slices, with `x9bz68vq_h25_core` improving from `0.1108426044` to `0.0991595710` at `a003` and `0.0900428281` at `a005`, and `horizon=25` improving from `0.1763402163` to `0.1758243812` at `a003` and `0.1754608149` at `a005`, but those gains were not enough to offset overall regressions | Keep `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1` as the canonical incumbent and move the next long-horizon overlay test to the `OSJL3A7Y / horizon=25` long-core bundle on the same parent |
| E047 | Milestone 5 | The `OSJL3A7Y / horizon=25` long-core bundle on top of the pentaslice incumbent might improve the remaining long-horizon watchlist enough to justify promotion after the X9 core branch failed | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_osjl_h25_core_a003_v1` and `..._a005_v1` in `src/ts_forecasting/baseline_registry.py`, added coverage in `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`36 passed in 1.70s`), then ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_osjl_h25_core_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_osjl_h25_core_a005_v1 --output-dir outputs/advanced_models/osjl_h25_core_warm_probe_exact_20260410` | Neither new probe beat the incumbent `0.1451607523`; `..._osjl_h25_core_a003_v1` scored `0.1446687857` and `..._osjl_h25_core_a005_v1` scored `0.1443124060`. The branch did improve its targeted stress slices, with `osjl3a7y_long_core` improving from `0.1235206266` to `0.1123322714` at `a003` and `0.1035016357` at `a005`, and `horizon=25` improving from `0.1763402163` to `0.1755701145` at `a003` and `0.1750117796` at `a005`, but those gains again failed to offset overall regressions | Keep `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1` as the canonical incumbent and treat the long-horizon warm-overlay family as near-plateaued unless a clearly differentiated short-horizon revisit proves justified |
| E048 | Milestone 5 | A final pentaslice `h=3`-only beta ratchet may be the last justified short-horizon drift revisit after the long-horizon overlay family plateaued | Added `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1` in `src/ts_forecasting/baseline_registry.py`, extended `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`36 passed in 1.68s`), ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1 --output-dir outputs/advanced_models/h13_pentaslice_h3_followup_exact_20260410`, reran the winner under `--cold-start-level triplet` into `outputs/advanced_models/h13_pentaslice_h3_followup_triplet_20260410`, and wrote `outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1_submission.csv` | `..._pentaslice_h3_beta_up_v1` became the new best exact offline model at `0.1451839276`, a gain of `+0.0000231753` over the prior incumbent; it improved `aggregate_cold_start_score` to `0.1602267731`, improved `aggregate_warm_start_score` to `0.0994653055`, improved `fold_02_03` to `0.0865730568`, improved `horizon=3` to `0.0596737026`, left `horizon=1`, `10`, and `25` unchanged, and held the same winner under the secondary triplet view | Promote `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1` as the new canonical incumbent and treat further unattended micro-tuning around this family as plateaued absent a materially different hypothesis or live leaderboard feedback |
| E049 | Milestone 5 | The exact Windows forward-CV path can be made cheaper without changing any metric or stress output | Added whole-frame static prediction support in `src/ts_forecasting/sequential.py`, propagated opt-in flags through the current predictor stack in `src/ts_forecasting/baselines.py` and `src/ts_forecasting/residuals.py`, fixed `WarmStartLastTargetBlendRegressor` to allow repeated future groups via `many_to_one`, rewrote `scripts/evaluate_baselines.py` to stream folds and update `stress` incrementally, added / extended `tests/test_sequential.py`, `tests/test_baselines.py`, and `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`40 passed in 1.74s`), and reran the incumbent pair into `outputs/advanced_models/runtime_verify_20260410/forward_cv_h360_f4_s360_code_sub_code` | Exact scores were unchanged versus the pre-refactor incumbent comparison while the evaluator no longer preloads all folds or retains a second full scored-frame copy for stress assembly; the parity rerun held `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1` at `0.1451839276` over `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1` at `0.1451607523` | Keep the runtime refactor as the default exact-CV path and use it for the first incumbent-based residual branch |
| E050 | Milestone 5 | A conservative strict-OOF unweighted Ridge residual on the incumbent’s `h=1/3` region may lift canonical score without colder-stress damage | Retargeted `scripts/evaluate_residual_models.py` to the incumbent summary `outputs/advanced_models/h13_pentaslice_h3_followup_exact_20260410/forward_cv_h360_f4_s360_code_sub_code/summary.json`, switched defaults to `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1`, removed fit-time `sample_weight`, added shared evaluation helpers in `src/ts_forecasting/evaluation.py`, added `tests/test_evaluate_residual_models.py`, wired residual stress diagnostics, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`43 passed in 1.81s`), and ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_residual_models.py --train-path data/train.parquet --output-dir outputs/residual_models` | `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1` became the new best exact offline branch at `0.1453567994`, a gain of `+0.0001728718` over the incumbent base; it improved `aggregate_cold_start_score` to `0.1603068083`, improved `aggregate_warm_start_score` to `0.1000169808`, improved `fold_02_03` to `0.0869802269`, improved `horizon=1` to `0.0368057749`, improved `horizon=3` to `0.0622159369`, and left the long-horizon watchlists unchanged | Promote the `h=1/3` strict-OOF residual as the new best offline branch and only consider follow-up branches that add value on top of it |
| E051 | Milestone 5 | After the `h=1/3` residual win, a specialist longwatch residual limited to `OSJL3A7Y` / `X9BZ68VQ` on horizons `10` and `25` may add targeted value without disturbing the rest of the stack | Added `ridge_oofnorm_longwatch_h1025_s003_q80_a100_unweighted_v1` to `scripts/evaluate_residual_models.py`, extended `tests/test_evaluate_residual_models.py`, reran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`44 passed in 1.70s`), and reran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_residual_models.py --train-path data/train.parquet --output-dir outputs/residual_models` with both residual configs active | The specialist longwatch residual regressed hard: canonical score fell to `0.1436092537`, `aggregate_cold_start_score` fell to `0.1587745279`, `aggregate_warm_start_score` fell to `0.0972590226`, `fold_02_03` fell to `0.0834505648`, `osjl3a7y_long_core` fell to `0.0988037879`, `x9bz68vq_h10_core` fell to `0.0846696769`, and `x9bz68vq_h25_core` fell to `0.0988467295` | Close the specialist longwatch ridge branch and do not spend more unattended CV budget on that family unless a materially different mechanism reopens it |
| E052 | Milestone 5 | Residual exact-CV results should be invariant to unrelated config additions, so sampling must not depend on incoming row order | Added a canonical pre-sample sort in `scripts/evaluate_residual_models.py` and `src/ts_forecasting/residuals.py`, added `test_sample_frame_is_stable_across_input_row_order` in `tests/test_evaluate_residual_models.py`, reran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`44 passed in 1.70s`), and reran the exact residual sweep | The promoted `h=1/3` residual score stabilized at `0.1453567994` with the longwatch config present, confirming that residual sampling is now reproducible across config-set changes | Treat `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1` at `0.1453567994` as the reproducible current best and prefer inference-path integration or a conditional expert blend over more residual sampling tweaks |
| E053 | Milestone 5 | The promoted `h=1/3` residual winner needs a reusable registry / prediction implementation so it can generate deterministic test-time outputs and serve as a parent for routed experts | Added `ConditionalLinearBlendRegressor` in `src/ts_forecasting/baselines.py`, added `NormalizedPrefixResidualCorrectionRegressor` in `src/ts_forecasting/residuals.py`, wired `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1`, `winner_longwatch_residual_raw_v1`, and `winner_plus_longwatch_expert_blend_v1` through `src/ts_forecasting/baseline_registry.py`, extended `tests/test_baselines.py`, `tests/test_residuals.py`, and `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests/test_baselines.py tests/test_residuals.py tests/test_evaluate_baselines.py` (`33 passed in 1.77s`), ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`47 passed in 1.74s`), and generated `outputs/submissions/ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1_submission.csv` plus `outputs/submissions/winner_plus_longwatch_expert_blend_v1_submission.csv` with `scripts/predict_baseline.py` | The promoted residual family now has a deterministic registry / CLI path, the routed blend also predicts end to end on the real test split, and the full suite stayed green at `47 passed in 1.74s` after the new registry surface landed | Use the new registry winner as the reusable prediction path and compare any routed specialist against it under the exact baseline evaluator before promoting another branch |
| E054 | Milestone 5 | A conservative conditional longwatch expert blend on top of the reusable `h=1/3` residual winner may lift the unresolved OSJL/X9 watchlists without paying back the short-horizon gains | Ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1 winner_longwatch_residual_raw_v1 winner_plus_longwatch_expert_blend_v1 --output-dir outputs/advanced_models/winner_longwatch_blend_exact_20260410` | The reusable winner baseline was the best of the routed registry variants at `0.1451217082`; the standalone longwatch expert regressed to `0.1441338366`, and the conditional blend also lost at `0.1449908861`. The blend nudged `x9bz68vq_h10_core` up to `0.0974936503` versus the winner’s `0.0951281345`, but it still regressed `aggregate_overall_score`, `aggregate_cold_start_score`, `aggregate_warm_start_score`, `fold_02_03`, `osjl3a7y_long_core`, and `x9bz68vq_h25_core`. The reusable winner itself also ran below the promoted strict-OOF result by `0.0002350912`, so the inference-safe analog is usable but not the new offline leader | Keep `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1` as the reusable packaging path, but do not promote the routed longwatch specialist or its conditional blend without a materially different expert that wins on canonical and colder diagnostics |
| E055 | Milestone 5 | The remaining gap between the promoted strict-OOF winner and the reusable registry analog may be caused by the baseline CLIs loading only `CONTEXT_COLUMNS`, which would starve residual registry baselines of their `feature_*` inputs | Added recursive `resolve_required_columns(...)` in `src/ts_forecasting/baseline_registry.py`, wired `scripts/evaluate_baselines.py` and `scripts/predict_baseline.py` through that resolver, extended `tests/test_evaluate_baselines.py`, ran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests/test_evaluate_baselines.py` (`16 passed in 1.37s`), confirmed the corrected registry path now sees `86` residual feature columns instead of just `ts_index`, and reran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1 ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1 --output-dir outputs/advanced_models/ridge_oofnorm_featureload_exact_20260410` | The exact reusable winner now matches the promoted strict-OOF result exactly at `0.1453567994`; the old lower registry scores were an artifact of feature-starved evaluation and prediction loads, not a real modeling gap | Keep the registry-driven required-column resolver in place and treat the older feature-starved exact outputs as stale |
| E056 | Milestone 5 | After fixing residual feature loading for the registry path, the routed longwatch specialist and conditional blend need one corrected exact rerun before they can stay demoted with confidence | Ran `& .\\.venv\\Scripts\\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1 winner_longwatch_residual_raw_v1 winner_plus_longwatch_expert_blend_v1 --output-dir outputs/advanced_models/winner_longwatch_blend_featureload_exact_20260410`, rewrote `outputs/submissions/ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1_submission.csv` and `outputs/submissions/winner_plus_longwatch_expert_blend_v1_submission.csv` with the corrected loader path, and reran `& .\\.venv\\Scripts\\python.exe -m pytest -q -s tests` (`50 passed in 2.30s`) | The corrected three-way rerun still left the winner on top at `0.1453567994`; `winner_longwatch_residual_raw_v1` fell to `0.1380775138`, and `winner_plus_longwatch_expert_blend_v1` only reached `0.1446841822`. The blend still regressed `fold_02_03` to `0.0854023498`, `osjl3a7y_long_core` to `0.1078167044`, `x9bz68vq_h10_core` to `0.0910181144`, and `x9bz68vq_h25_core` to `0.1098746042` | Keep the longwatch specialist and its routed blend closed; the corrected loader removed the packaging mismatch but did not rescue that branch |

## Commands That Matter

- Read order at session start:
  - `prompt.md`
  - `plans.md`
  - `documentation.md`
  - `implement.md`
- Default Python environment:
  - `& .\.venv\Scripts\python.exe`
- Default dependency install:
  - `python -m pip install -r requirements.txt`
- Local inspection reference used during setup:
  - `. .venv/bin/activate && python -c "import duckdb; ..."`
- Run tests:
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests`
- Regenerate profiles:
  - `. .venv/bin/activate && python scripts/profile_data.py --output-dir outputs/profiles`
- Evaluate current baselines:
  - `. .venv/bin/activate && python scripts/evaluate_baselines.py --train-path data/train.parquet --holdout-steps 30 --output-dir outputs/baselines`
- Evaluate the current best offline proxy:
  - `. .venv/bin/activate && python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --output-dir outputs/baselines`
- Evaluate the rebuilt raw residual branch and its clean horizon blend without disturbing the default baseline summary:
  - `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend ridge_triweak_h13_tw720_s022_q90 ridge_triweak_h13_tw720_s022_q90_horizon_blend --output-dir outputs/advanced_models`
- Evaluate the warm last-target overlay against the old baseline incumbent:
  - `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend smoothed_stable_horizon_blend_last_target_rules --output-dir outputs/advanced_models`
- Evaluate the recent-delta overlay against the warm last-target incumbent:
  - `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_rules smoothed_stable_horizon_blend_last_target_recent_delta_v1 --output-dir outputs/advanced_models`
- Evaluate the current best stacked incumbent against its recent-delta parent:
  - `. .venv/bin/activate && PYTHONPATH=src python scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output-dir outputs/stacked_models`
- Evaluate the exact clipped/gated delta short list on the real parquet files:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_hbeta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_clip_osjlx9_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output-dir outputs/advanced_models/delta_sweep_exact_20260410_code_sub_code`
- Re-run the stress-vetted delta short list under the colder triplet view:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level triplet --baselines smoothed_stable_horizon_blend_last_target_recent_delta_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output-dir outputs/advanced_models/delta_sweep_triplet_20260410_shortlist`
- Evaluate the first OSJL/X9 warm-probe bundle:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_osjl_dppu10_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_osjl_ph10_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_osjl_pz10_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_x9_ph10_a003_v1 --output-dir outputs/advanced_models/osjl_x9_warm_probe_exact_20260410`
- Evaluate the short-horizon `h=1/3` follow-up branch:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_dppu_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1 --output-dir outputs/advanced_models/h13_drift_exact_20260410`
- Re-run the promoted `h=1/3` winner under the triplet stress view:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level triplet --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1 --output-dir outputs/advanced_models/h13_drift_triplet_20260410`
- Evaluate the local `h=1/3` follow-up sweep around the tri-slice winner:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_lo_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_rw360_v1 --output-dir outputs/advanced_models/h13_followup_exact_20260410`
- Re-run the local `h=1/3` winner under the triplet stress view:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level triplet --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 --output-dir outputs/advanced_models/h13_followup_triplet_20260410`
- Evaluate the widened all-sub-category `h=1/3` branch:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 --output-dir outputs/advanced_models/h13_pentaslice_exact_20260410`
- Re-run the widened `h=1/3` winner under the triplet stress view:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level triplet --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_trislice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 --output-dir outputs/advanced_models/h13_pentaslice_triplet_20260410`
- Evaluate the pentaslice `X9BZ68VQ / horizon=25` core warm-probe bundle:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_x9_h25_core_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_x9_h25_core_a005_v1 --output-dir outputs/advanced_models/x9_h25_core_warm_probe_exact_20260410`
- Evaluate the pentaslice `OSJL3A7Y / horizon=25` long-core warm-probe bundle:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_osjl_h25_core_a003_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_osjl_h25_core_a005_v1 --output-dir outputs/advanced_models/osjl_h25_core_warm_probe_exact_20260410`
- Evaluate the final pentaslice `h=3` beta-up follow-up:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1 --output-dir outputs/advanced_models/h13_pentaslice_h3_followup_exact_20260410`
- Re-run the final pentaslice `h=3` winner under the triplet stress view:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level triplet --baselines smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_beta_hi_v1 smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1 --output-dir outputs/advanced_models/h13_pentaslice_h3_followup_triplet_20260410`
- Generate the current best-known submission-format predictions:
  - `. .venv/bin/activate && python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend --output outputs/submissions/smoothed_stable_horizon_blend_submission.csv`
- Generate the warm last-target overlay submission-format predictions:
  - `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_rules --output outputs/submissions/smoothed_stable_horizon_blend_last_target_rules_submission.csv`
- Generate the recent-delta incumbent submission-format predictions:
  - `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_recent_delta_v1 --output outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_v1_submission.csv`
- Generate the current best stacked submission-format predictions:
  - `& .\.venv\Scripts\python.exe scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1 --output outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1_submission.csv`
- Generate the stress-vetted delta-parent submission-format predictions:
  - `& .\.venv\Scripts\python.exe scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1 --output outputs/submissions/smoothed_stable_horizon_blend_last_target_recent_delta_k7_v1_submission.csv`
- Generate the rebuilt residual-plus-horizon blend predictions:
  - `. .venv/bin/activate && PYTHONPATH=src python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline ridge_triweak_h13_tw720_s022_q90_horizon_blend --output outputs/submissions/ridge_triweak_h13_tw720_s022_q90_horizon_blend_submission.csv`
- Analyze the saved validation predictions:
  - `. .venv/bin/activate && python scripts/analyze_validation_errors.py --summary-path outputs/baselines/forward_cv_h360_f4_s360_code_sub_code/summary.json --train-path data/train.parquet --baseline smoothed_stable_group_mean_pw100 --output-dir outputs/analysis --top-k 3`
- Analyze the canonical weak slices in more detail:
  - `. .venv/bin/activate && python scripts/analyze_validation_errors.py --summary-path outputs/baselines/forward_cv_h360_f4_s360_code_sub_code/summary.json --train-path data/train.parquet --baseline smoothed_stable_group_mean_pw100 --output-dir outputs/analysis --top-k 10`
- Evaluate residual models on top of the best grouped baseline:
  - `. .venv/bin/activate && python scripts/evaluate_residual_models.py --base-summary-path outputs/baselines/forward_cv_h360_f4_s360_code_sub_code/summary.json --train-path data/train.parquet --base-baseline smoothed_stable_group_mean_pw100 --output-dir outputs/residual_models`
- Evaluate the current incumbent-based strict-OOF residual sweep with stress diagnostics:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_residual_models.py --train-path data/train.parquet --output-dir outputs/residual_models`
- Evaluate the repaired causal weighted LightGBM residual branch:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411`
- Evaluate the current best causal LightGBM residual winner:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411 --name-suffix leaf31_lr002_mc400_ff07_bf07_ra05_rl2 --num-leaves 31 --learning-rate 0.02 --min-child-samples 400 --feature-fraction 0.7 --bagging-fraction 0.7 --bagging-freq 1 --reg-alpha 0.5 --reg-lambda 2.0`
- Evaluate the latest repo-leading causal LightGBM residual winner:
- `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411 --name-suffix h131025_leaf31_lr002_mc520_ff06_bf07_ra20_rl5 --residual-horizons 1 3 10 25 --num-leaves 31 --learning-rate 0.02 --min-child-samples 520 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --reg-alpha 2.0 --reg-lambda 5.0`
- Evaluate the reusable residual winner plus the routed longwatch specialist and blend under the exact baseline evaluator:
  - `& .\.venv\Scripts\python.exe scripts/evaluate_baselines.py --train-path data/train.parquet --test-path data/test.parquet --holdout-steps 360 --n-folds 4 --step-size 360 --min-train-steps 2000 --cold-start-level code_sub_code --baselines ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1 winner_longwatch_residual_raw_v1 winner_plus_longwatch_expert_blend_v1 --output-dir outputs/advanced_models/winner_longwatch_blend_exact_20260410`
- Inspect the promoted incumbent-based residual summary:
  - `outputs/residual_models/forward_cv_h360_f4_s360_code_sub_code_oof_norm_incumbent_h13_unweighted/summary.json`
- Inspect the routed winner / longwatch blend comparison summary:
  - `outputs/advanced_models/winner_longwatch_blend_exact_20260410/forward_cv_h360_f4_s360_code_sub_code/summary.json`
- Inspect the strict-OOF normalized residual artifacts and best shrink sweep:
  - `outputs/residual_models/forward_cv_h360_f4_s360_code_sub_code_oof_norm/summary.json`
  - `outputs/residual_models/forward_cv_h360_f4_s360_code_sub_code_oof_norm/posthoc_rescale_sweep.json`
  - `outputs/residual_models/forward_cv_h360_f4_s360_code_sub_code_oof_norm/ridge_oofnorm_triweak_h13_tw720_s003_q85_posthoc/summary.json`
- Generate the reusable promoted residual winner predictions:
  - `& .\.venv\Scripts\python.exe scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1 --output outputs/submissions/ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1_submission.csv`
- Generate the routed longwatch blend predictions:
  - `& .\.venv\Scripts\python.exe scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline winner_plus_longwatch_expert_blend_v1 --output outputs/submissions/winner_plus_longwatch_expert_blend_v1_submission.csv`
- Generate the previous best grouped-only submission-format predictions:
  - `. .venv/bin/activate && python scripts/predict_baseline.py --train-path data/train.parquet --test-path data/test.parquet --baseline smoothed_stable_group_mean_pw100 --output outputs/submissions/smoothed_stable_group_mean_pw100_submission.csv`
- Audit competition semantics:
  - `. .venv/bin/activate && python scripts/audit_semantics.py --train-path data/train.parquet --test-path data/test.parquet --output-dir outputs/semantics`
- Run the current smoke test suite:
  - `. .venv/bin/activate && pytest -q -s tests`

## Notes For Future Updates

- Keep this file append-only where practical.
- Record failures, not just successes.
- When a milestone completes, update:
  - milestone board,
  - decisions,
  - experiment log,
  - commands that matter,
  - next actions.

## Tooling Update (`2026-04-10 UTC`)

- Added Kaggle MCP to the local Codex client config at `C:\Users\tilav\.codex\config.toml` using the streamable HTTP endpoint `https://www.kaggle.com/mcp`.
- Enabled `[features].rmcp_client = true` alongside the new `[mcp_servers.kaggle]` entry so OAuth-capable streamable HTTP MCP servers can authenticate from Codex.
- Source used for the setup contract: Kaggle MCP docs at `https://www.kaggle.com/docs/mcp`.
- Expected follow-up after this config change:
  - restart Codex so the new MCP server is loaded,
  - authorize the Kaggle server in-client if protected tools/resources are needed,
  - if OAuth is unavailable in a given client build, fall back to Kaggle bearer-token auth with a token that starts with `KGAT`.

## Submission-Proxy Update (`2026-04-11 UTC`)

- Added a durable pseudo-public benchmark at `scripts/evaluate_lgbm_submission_proxy.py`, with coverage in `tests/test_evaluate_lgbm_submission_proxy.py`. The script replays the checked-in `scripts/predict_lgbm.py` submission path on a tail holdout with the same length as real test, then scores both the full tail and repeated 24% pseudo-public subsets.
- Validation after the new script and warm-only gating work:
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests/test_evaluate_lgbm.py tests/test_predict_lgbm.py tests/test_evaluate_lgbm_submission_proxy.py`
  - result: `21 passed in 4.30s`
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests`
  - result: `80 passed, 1 warning in 5.75s`
- Pseudo-public tail benchmark for the current public-winning residual LGBM branch (`mc520/msg020/ra25/rl408`):
  - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm_submission_proxy.py --summary-output outputs/lgbm_submission_proxy/current_winner_proxy.json --pseudo-test-steps 775 --n-bootstraps 50 --residual-horizons 1 3 10 25 --num-leaves 31 --learning-rate 0.02 --min-child-samples 520 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --min-split-gain 0.2 --reg-alpha 2.5 --reg-lambda 4.08`
  - result: full tail `0.1054061164`; random 24% bootstrap mean `0.1051657368`; stratified 24% bootstrap mean `0.1054348098`
  - coverage on that pseudo-test tail: `1,156,330 / 1,312,170` rows unseen at `(code, sub_code)` = `88.1235%`, much closer to real test than the old 4-fold CV
  - calibration under the tail proxy collapsed to `best_iteration=1`, which is strong evidence that the 4-fold aggregate selector is materially overvaluing residual correction strength on this regime
- Tail incumbent-only check:
  - command: inline SequentialInferencePipeline replay on prefix `ts<=2826`, holdout `2827..3601`
  - result: `0.1058854687`, slightly better than the current all-row residual branch on the pseudo-public tail
- Implemented a new causal gating option: `--apply-residual-only-warm`
  - wired through `scripts/evaluate_lgbm.py`, `scripts/predict_lgbm.py`, and the new submission-proxy script
  - behavior: keep training the same residual model, but only apply its correction on rows whose `(code, sub_code)` key is already seen in the training prefix
  - rationale: the pseudo-public tail showed the residual branch hurting cold rows more than it helped warm rows
- Warm-only branch results:
  - 4-fold exact CV command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411 --name-suffix h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408 --apply-residual-only-warm --residual-horizons 1 3 10 25 --num-leaves 31 --learning-rate 0.02 --min-child-samples 520 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --min-split-gain 0.2 --reg-alpha 2.5 --reg-lambda 4.08`
  - exact CV result: `0.1519289421`, below the current canonical winner `0.1600435435`
  - pseudo-public tail command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm_submission_proxy.py --summary-output outputs/lgbm_submission_proxy/current_winner_warmonly_proxy.json --pseudo-test-steps 775 --n-bootstraps 50 --apply-residual-only-warm --residual-horizons 1 3 10 25 --num-leaves 31 --learning-rate 0.02 --min-child-samples 520 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --min-split-gain 0.2 --reg-alpha 2.5 --reg-lambda 4.08`
  - pseudo-public result: `0.1071458916`, better than both incumbent-only (`0.1058854687`) and the same branch without warm-only gating (`0.1054061164`)
  - live submission artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_warmonly_submission.csv`
  - live submit message: `warm-only residual proxy test`
  - first upload attempt failed with a Kaggle API connection timeout; retry succeeded
  - live public score: `0.1285`
- Decision update:
  - keep the original checked-in residual submit as the public leader at `0.2045`; the warm-only branch is now closed for public selection despite winning on the new pseudo-public tail proxy
  - the pseudo-public benchmark is still worth keeping because it exposed a real regime split, but it is not aligned enough to replace direct public feedback as the selector
  - current strongest conclusion: the remaining gap is not just “submission path correctness” anymore; it is structural mismatch between available offline selectors and the hidden scored public subset
- Immediate next actions:
  - keep `scripts/evaluate_lgbm_submission_proxy.py` in the loop as a diagnostic, not as the sole promotion rule
  - treat `--apply-residual-only-warm` as a useful closed experiment: it improved the tail proxy and still failed public
  - spend the next branch budget on selectors and model families that can explain why public prefers the original all-row residual correction while both the incumbent-only and warm-only variants look better on the test-like tail proxy

## Public-Gated Update (`2026-04-11 UTC`, later)

- Fixed the new horizon-scoped warm-gate regression in `scripts/evaluate_lgbm.py`: `run_fold()` now builds fold-local seen-pair state whenever `--residual-warm-only-horizons` is active, not only when the old all-horizons warm gate flag is set.
- Added a direct regression for that bug in `tests/test_evaluate_lgbm.py`; targeted validation:
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests/test_evaluate_lgbm.py tests/test_predict_lgbm.py tests/test_evaluate_lgbm_submission_proxy.py`
  - result: `24 passed in 4.84s`
- Full smoke validation after the new gating work and submission generation:
  - `& .\.venv\Scripts\python.exe -m pytest -q -s tests`
  - result: `83 passed, 1 warning in 6.49s`
- New `h=1/3` cold-gated branch (`warmh13`) exact CV:
  - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm.py --mode residual --output-dir outputs/lgbm_models_causal_20260411 --name-suffix h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408 --residual-warm-only-horizons 1 3 --residual-horizons 1 3 10 25 --num-leaves 31 --learning-rate 0.02 --min-child-samples 520 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --min-split-gain 0.2 --reg-alpha 2.5 --reg-lambda 4.08`
  - result: `0.1605824443` overall
  - artifact: `outputs/lgbm_models_causal_20260411/forward_cv_h360_f4_s360_code_sub_code/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_warmh13_summary.json`
- Warmh13 micro-sweep follow-up:
  - five closely related runs stayed in a very tight band
  - `msg015` narrowly led the control `0.1605767426`, making `0.1605824443` the current best exact offline score
- New `warmh13` pseudo-public tail benchmark:
  - command: `& .\.venv\Scripts\python.exe scripts/evaluate_lgbm_submission_proxy.py --summary-output outputs/lgbm_submission_proxy/warmh13_proxy.json --pseudo-test-steps 775 --n-bootstraps 50 --residual-warm-only-horizons 1 3 --residual-horizons 1 3 10 25 --num-leaves 31 --learning-rate 0.02 --min-child-samples 520 --feature-fraction 0.6 --bagging-fraction 0.7 --bagging-freq 1 --min-split-gain 0.2 --reg-alpha 2.5 --reg-lambda 4.08`
  - result: full tail `0.1062599303`; random 24% bootstrap mean `0.1060351583`; stratified 24% bootstrap mean `0.1063143679`
  - note: the tail proxy did not beat warm-only, but it stayed non-regressive and exact CV preferred it
- New `warmh13` live submission:
  - artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_warmh13_submission.csv`
  - message: `short horizon cold gate test`
  - public score: `0.2099`
- Interpretation update:
  - this is the strongest evidence so far that exact 4-fold CV is still the better selector than the test-like tail proxy when the two disagree
  - the structural improvement that translated publicly was narrower than the warm-only branch: gate only cold `h=1/3`, keep the all-row residual path on `h=10/25`
  - `warmh13` is now the active repo center for both offline and live work
- Routed-expert follow-up:
  - best pseudo-public routed policy among the frozen experts was `lgbm_routed_short_horizon_cold_gate_v1`, with tail `0.1074813111`
  - exact 4-fold rerun for that route reached only `0.1590558555`
  - live submission artifact: `outputs/submissions/lgbm_routed_short_horizon_cold_gate_v1_submission.csv`
  - live message: `routed expert cold gate test`
  - live public score: `0.1990`
  - decision: close the routed-expert branch for promotion; it improved the proxy and still lost to the simpler `warmh13` single-policy branch
- Narrower-gate follow-up:
  - exact CV for `warmh1`: `0.1605868575`, slightly above `warmh13`
  - exact CV for `warmh3`: `0.1600371591`, effectively back at the old all-row level
  - `warmh1` pseudo-public tail result: `0.1058076277`, below `warmh13`
  - live `warmh1` submission artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_warmh1_submission.csv`
  - live message: `cold h1 gate test`
  - live public score: `0.2074`
  - decision: keep `warmh13` as the public leader and close `warmh1` / `warmh3` for promotion; exact-CV gains that small are not enough to override the live result
- Current next action:
  - keep `aggregate_overall_score` canonical and treat the tail proxy as diagnostic-only
  - stay on the `warmh13` center and spend the next loop on tighter parameter or calibration follow-ups around that branch before reopening broader routing ideas

## Tooling Note (`2026-04-13 UTC`)

- Fetched and followed the external install instructions from `https://raw.githubusercontent.com/obra/superpowers/refs/heads/main/.codex/INSTALL.md`.
- Installed `obra/superpowers` by cloning `https://github.com/obra/superpowers.git` into `/root/.codex/superpowers`.
- Created the native Codex skill-discovery symlink at `/root/.agents/skills/superpowers -> /root/.codex/superpowers/skills`.
- Verified the symlink with `ls -la /root/.agents/skills/superpowers`.
- Checked `/root/.codex/AGENTS.md` for the legacy `superpowers-codex bootstrap` block and found no matching legacy bootstrap content to remove.

## Cloud Migration Update (`2026-04-13 UTC`)

- The active cloud instance now reports `AMD EPYC 7713P` CPU capacity with `128` logical CPUs, `251 GiB` RAM visible to the OS, `NVIDIA RTX A5000` GPU memory at `24564 MiB`, and `WD_BLACK SN850X` storage.
- The repo-local `.venv` is already bootstrapped from `requirements.txt`, so Python work should continue from that environment rather than recreating the toolchain.
- LightGBM runtime defaults on this host cap auto `num_threads` at `64` and enable `force_col_wise` on many-core hosts; expect higher CPU throughput than the old workstation, but do not assume linear scaling across all logical CPUs.
- Added host-aware LightGBM runtime controls in `src/ts_forecasting/lgbm_models.py` and exposed `--num-threads`, `--device-type`, and `--seed` overrides through both `scripts/evaluate_lgbm.py` and `scripts/predict_lgbm.py`.
- Validation after the runtime change:
  - `.venv/bin/python -m pytest -q`
  - result: `91 passed, 1 warning in 7.95s`
- Backend/runtime probes on this host:
  - tiny LightGBM smoke test: `device_type=gpu` trained successfully, while `device_type=cuda` failed with `CUDA Tree Learner was not enabled in this build`
  - synthetic `200000 x 86` CPU benchmark before the GPU leg stalled: `num_threads=32 -> 2.106s`, `64 -> 1.947s`, `96 -> 3.486s`, which supports keeping the default cap at `64`
- `data/train.parquet` and `data/test.parquet` are restored on this instance, so data-dependent runs can proceed again.
- Kaggle token auth now works:
  - `.venv/bin/python -m kaggle competitions files -c ts-forecasting`
  - result: authenticated competition API calls succeed
- CPU cleanup and host check:
  - stray benchmark processes were killed before the latest throughput rerun
  - the host is not overloaded; the current bottleneck is the incumbent OOF path plus low effective parallelism
- Fast-path work:
  - `--preload-train-frame` loads the full train parquet once and slices outer folds from memory
  - `--precompute-oof-incumbent` builds the strict incumbent OOF series once on the full train frame and reuses it across folds
- Next score-moving batch should stay on the `warmh13` center with narrow sweeps around `reg_lambda`, `min_split_gain`, `reg_alpha`, `feature_fraction`, and the calibrated full-train refit window.

## Exact-CV LightGBM Throughput Optimization (`2026-04-13 UTC`)

- Added a safe optional evaluator path in `scripts/evaluate_lgbm.py` for exact-CV LightGBM runs:
  - `--preload-train-frame` loads the full train parquet once and slices outer folds from memory
  - `--precompute-oof-incumbent` builds the strict incumbent OOF series once on the full train frame and reuses it across folds
  - `run_fold()` now accepts `precomputed_train_oof`, reuses it for train rows, and also reuses it for validation rows when the global OOF series already covers that outer fold
- Added `load_fold_frames()` so the preload path can slice fold train / valid frames deterministically from a single in-memory frame.
- Added an ordered fast path to `SequentialInferencePipeline.predict(..., already_sorted=True)` and switched the normalized residual incumbent's internal OOF builder to use it.
- Removed the per-inner-fold `validation_frame.merge(..., on=id)` round-trip in `NormalizedPrefixResidualCorrectionRegressor._build_internal_oof_training_pool`; sorted validation slices now receive `base_prediction` positionally after a single explicit sort.
- Default behavior is unchanged when the new flags are not used.
- Verification:
  - `. .venv/bin/activate && python -m pytest -q -s tests/test_evaluate_lgbm.py`
  - result: `14 passed in 1.71s`
  - `. .venv/bin/activate && python -m pytest -q -s tests/test_residuals.py`
  - result: `10 passed in 2.21s`
  - `. .venv/bin/activate && python -m pytest -q -s tests/test_sequential.py -k already_sorted_fast_path`
  - result: `1 passed, 3 deselected in 0.49s`
  - `. .venv/bin/activate && python -m pytest -q`
  - result: `91 passed, 1 warning in 7.95s`
- Notes:
  - the new path preserves causality because only the fold-local training slice is used for the residual model, while the incumbent OOF series is precomputed once from the full training prefix and then sliced by index
  - the validation helper tests lock both the preload slicing behavior and the validation-OOF reuse path so future refactors do not silently reintroduce per-fold parquet reads or redundant incumbent refits
  - the residual hot path still spends most of its time inside nested incumbent fit/predict work, but the remaining avoidable alignment overhead is now lower: no extra validation merge in the inner OOF builder and no extra sort when the caller has already ordered the batch

## Residual Scale Lookup Optimization (`2026-04-13 UTC`)

- Replaced the merge-based `attach_residual_scales()` path in `src/ts_forecasting/residuals.py` with MultiIndex-based lookup alignment.
- The new path preserves the caller's index and row order while removing two more hash joins from the incumbent residual hot path.
- Added a regression test in `tests/test_residuals.py` proving that `attach_residual_scales()` preserves input index order.
- Verification:
  - `. .venv/bin/activate && python -m pytest -q -s tests/test_residuals.py`
  - result: `11 passed in 2.60s`
  - `. .venv/bin/activate && python -m pytest -q`
  - result: `92 passed, 1 warning in 7.46s`

## Fast Warmh13 Sweep (`2026-04-13 UTC`)

- Completed a fast-path exact-CV sweep around the current `warmh13` center using the preload + precomputed-OOF path.
- Results ranked by canonical exact offline score:
  - incumbent baseline: `0.1605824443` from `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_warmh13`
  - closest fast retries:
    - `ra=2.65, rl=4.08`: `0.1605697414`
    - `min_split_gain=0.16, rl=4.04`: `0.1605673840`
  - clear regressions:
    - `msg014, rl=4.12`: `0.1595300221`
    - `msg013, rl=4.16`: `0.1595265742`
    - `mc=500`: `0.1580188549`
    - `ff=0.58`: `0.1558618628`
    - `mc=540`: `0.1547694395`
    - `bf=0.75`: `0.1525401610`
    - `ff=0.64`: `0.1498215599`
- Takeaway: the `warmh13/msg015` neighborhood remains extremely tight and the current center still leads; modest moves in `feature_fraction`, `bagging_fraction`, or `min_child_samples` degraded quickly.

## Fast Warmh13 Sweep 3 And Timed Rerun (`2026-04-13 UTC`)

- Completed a broader structural probe set around the `msg015` center:
  - timed exact rerun on the newest fast path: `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_fastoofv4_warmh13`
  - `warmh1310`
  - `leaf47 / lr0.015 / mc700`
  - `leaf63`
  - full warm-only gating across all residual horizons
  - `h13`-only residual horizon rollback
- Results:
  - timed rerun: `0.1605798078`, effectively score-identical to the incumbent `0.1605824443`
  - `warmh1310`: `0.1588627102`
  - `leaf47 / lr0.015 / mc700`: `0.1556729999`
  - `leaf63`: `0.1550824498`
  - full warm-only gating: `0.1519283464`
  - `h13`-only: `0.1459111921`
- Throughput result from the timed rerun:
  - shell elapsed wall time: `499.090s`
  - in-script `Total time`: `286.0s`
  - this is a major reduction from the earlier exact-CV runtime in the `~1765s` range for the old path while preserving the incumbent score neighborhood
- Takeaway:
  - the current fast path is materially healthier now
  - broader gating changes and larger-tree shape changes regressed
  - the next search branch should shift to learning-rate / boosting-budget shape rather than more gating or local regularization nudges

## Fast Path Promotion And GPU Probe (`2026-04-13 UTC`)

- Added another inner-loop throughput patch in `NormalizedPrefixResidualCorrectionRegressor._build_internal_oof_training_pool`:
  - sort the training frame once by `[ts_index, id]`
  - use `np.searchsorted()` to build prefix and validation slices positionally
  - avoid per-fold boolean scans and per-fold validation sorting
- Added regression tests in `tests/test_residuals.py` to lock:
  - stable sorted validation ordering in the internal OOF pool
  - only one `[ts_index, id]` sort call in the internal OOF builder
- Verification after the slice patch:
  - `. .venv/bin/activate && python -m pytest -q`
  - result: `94 passed, 1 warning in 9.70s`
- Timed exact rerun on the promoted `msg015` center using the newest fast path:
  - model: `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_fastoofv5_warmh13`
  - exact score: `0.1605824514`
  - cold-start: `0.1720937309`
  - warm-start: `0.1283100077`
  - in-script `Total time`: `136.2s`
  - shell wall time: `ELAPSED=403.000`
- This confirms the newest CPU path preserves the incumbent score while materially reducing end-to-end runtime versus the older exact-CV path.
- Learning-rate / shape probes that followed were all worse:
  - `lr=0.015`: `0.1553263883`
  - `lr=0.01`: `0.1563091237`
  - `leaf15 / lr=0.03`: `0.1555789758`
  - `max_depth=6`: `0.1572264993`
- GPU probe result:
  - attempted the incumbent exact-CV config with `--device-type gpu`
  - LightGBM failed on fold 1 with `Check failed: (best_split_info.right_count) > (0)`
  - current conclusion: stay on CPU for exact-CV runs on this host unless the GPU build or parameters are stabilized in a separate dedicated benchmark

## Cached Incumbent OOF Reuse (`2026-04-13 UTC`)

- Added a reusable on-disk cache for the full-train incumbent OOF series in `scripts/evaluate_lgbm.py`:
  - `build_precomputed_oof_cache_path()`
  - `write_precomputed_oof_cache()`
  - `load_precomputed_oof_cache()`
  - `resolve_precomputed_train_oof()`
- The evaluator now persists and reloads the strict incumbent OOF predictions under `outputs/oof_cache/` instead of rebuilding them on every cached exact-CV run.
- Warmed the cache on the cloud host:
  - artifact: `outputs/oof_cache/train_ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1_h360_s360_m1000.parquet`
  - observed cache stats: `5,337,414` rows, `4,101,632` non-null incumbent predictions
- Verification:
  - `. .venv/bin/activate && python -m pytest -q`
  - result: `96 passed, 1 warning in 7.31s`
- Takeaway:
  - cached exact-CV runs now skip the expensive incumbent rebuild and print `Loading cached incumbent OOF predictions...`
  - the remaining dominant wall time is the fold-local LightGBM fit path rather than incumbent plumbing

## Cached Regime Sweep (`2026-04-13 UTC`)

- Completed a first cache-backed regime sweep around the promoted `warmh13/msg015` center.
- Results:
  - weighted no-bagging (`bf=1.0`, `bagging_freq=0`): `0.1484776716`
  - unweighted: `0.0`
  - unweighted + no-bagging: `0.0`
- Takeaway:
  - unweighted LightGBM is decisively broken for this branch on exact CV
  - no-bagging is also clearly inferior, so the current `bagging_fraction=0.7`, `bagging_freq=1` policy stays promoted

## Cached Regularization Sweep (`2026-04-13 UTC`)

- Completed a second cache-backed exact-CV sweep focused on the last plausible local regularization pocket.
- Results:
  - `msg005`: `0.1605598667`
  - `msg010`: `0.1605572905`
  - `ra15`: `0.1605636504`
  - `ra35`: `0.1600938127`
  - `rl35`: `0.1572169944`
  - `rl50`: `0.1600290404`
- Takeaway:
  - none of the cache-backed local regularization retries beat the incumbent `0.1605824443`
  - the single-policy `warmh13` residual basin now looks effectively plateaued; the next materially different branch should be a new model family or blend/stacking path rather than more `reg_alpha` / `reg_lambda` / `min_split_gain` jitter

## Prepared Feature Reuse Patch (`2026-04-13 UTC`)

- Added a reusable prepared-feature representation in `src/ts_forecasting/lgbm_models.py`:
  - `PreparedFeatureFrame` stores the base model-input frame, feature ordering, and category maps
  - `prepare_feature_frame()` builds the reusable representation once from a raw frame
  - `build_feature_frame()` now delegates to the prepared representation so the legacy API stays unchanged
- Extended `LGBMForecaster` with optional prepared-frame inputs:
  - `fit(..., prepared_train_frame=..., prepared_eval_frame=...)`
  - `predict(..., prepared_frame=...)`
  - callers that do not pass the new kwargs keep the existing behavior
- Wired `scripts/evaluate_lgbm.py` to reuse prepared features when the full train frame is preloaded:
  - the full train feature matrix is prepared once
  - outer folds slice that prepared matrix by row index instead of rebuilding the base feature frame each time
  - residual / stacked paths still layer `incumbent_pred` on top of the prepared base frame only when needed
- Validation:
  - `. .venv/bin/activate && python -m pytest -q tests/test_lgbm_models.py tests/test_evaluate_lgbm.py tests/test_evaluate_lgbm_submission_proxy.py`
  - result: `40 passed, 1 warning in 4.65s`
- Timed exact rerun on the promoted `msg015` center using the prepared-feature path:
  - model: `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_fastoofv6_warmh13`
  - exact score: `0.1605824485`
  - cold-start: `0.1720937271`
  - warm-start: `0.1283100077`
  - in-script `Total time`: `243.4s`
- Takeaway:
  - the prepared-feature path preserved the incumbent exact score
  - it reduced exact-CV runtime again versus `fastoofv5` (`243.4s` vs `403.0s` wall time on the same host)

## Residual Structural Follow-Up (`2026-04-13 UTC`)

- Closed the remaining exact-CV structural regularizer sweep around the promoted `warmh13/msg015` center.
- Results:
  - `hess5`: `0.1605763229`
  - `hess10`: `0.1596527584`
  - `hess25`: `0.1592217965`
  - `hess50`: `0.1573165585`
- Additional exploratory branches were cut early after clearly bad fold-level signal:
  - `leaf47`
  - `leaf47 + hess10`
  - `stacked` LightGBM retries
- Takeaway:
  - no exact-CV structural variant beat the incumbent
  - the single-policy residual exact basin remains effectively plateaued

## Submission Proxy Runtime Overrides (`2026-04-13 UTC`)

- Fixed `scripts/evaluate_lgbm_submission_proxy.py` so it now accepts the same runtime overrides as the main evaluator / predictor:
  - `--num-threads`
  - `--device-type`
  - `--seed`
- Added a regression test in `tests/test_evaluate_lgbm_submission_proxy.py` covering those flags.
- Verification:
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_lgbm_submission_proxy.py -k 'runtime_override_flags or help_runs'`
  - result: `2 passed, 4 deselected in 5.59s`

## Calibration Window Sweep (`2026-04-13 UTC`)

- Shifted the score-improvement branch from exact-CV regularizer sweeps to submission-proxy calibration-window sweeps on the promoted `warmh13/msg015` center.
- Results on the pseudo-public tail proxy:
  - `cal360`: `0.1062599303`
  - `cal480`: `0.1093652157`
  - `cal510`: `0.1067117192`
  - `cal525`: `0.1062599303`
  - `cal540`: `0.1101005573`
  - `cal555`: `0.1081953255`
  - `cal570`: `0.1065917579`
  - `cal600`: `0.1062599303`
  - `cal720`: `0.1081955248`
- Best-iteration pattern from the calibration split:
  - `cal360`: `1`
  - `cal480`: `20`
  - `cal510`: `3`
  - `cal525`: `1`
  - `cal540`: `52`
  - `cal555`: `12`
  - `cal570`: `4`
  - `cal600`: `1`
  - `cal720`: `14`
- Takeaway:
  - `540` is the clear local optimum in the tested neighborhood
  - both narrower and wider nearby windows quickly collapse back toward the weak `1`-tree regime

## Near-Tied Config Proxy Check (`2026-04-13 UTC`)

- Compared the top near-tied exact-CV configs at the winning `cal540` proxy window:
  - promoted `msg015/rl408`: `0.1101005573`
  - control `msg020/rl408`: `0.1101005573`
  - alternate `rl405`: `0.1101005311`
- Takeaway:
  - the proxy does not materially prefer the near-tied exact-CV alternatives over the promoted `msg015` center
  - keep `msg015` as the active center because it still leads exact CV and matches the best proxy result

## Cal540 Submission Artifact (`2026-04-13 UTC`)

- Generated a new full-train submission-format artifact from the best proxy branch:
  - artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540_submission.csv`
  - metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540_submission_metadata.json`
- The full-train calibration metadata selected `best_iteration=156` on the actual training/test split.
- Current recommendation:
  - treat the `cal540` artifact as the best available submission candidate on this host until a new proxy branch clears `0.1101005573`

## Calibration Refinement And Blend (`2026-04-13 UTC`)

- Refined the `cal540` neighborhood after the first proxy win:
  - `cal510`: `0.1067117192`
  - `cal525`: `0.1062599303`
  - `cal555`: `0.1081953255`
  - `cal570`: `0.1065917579`
- These refinements confirm the local shape is sharp: `540` remains the clear peak, while nearby windows quickly fall back toward the weak low-tree regime.
- Submission-format component artifacts:
  - `cal540` submission: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540_submission.csv`
  - `cal540` metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540_submission_metadata.json`
  - full-train calibration picked `best_iteration=156`
  - `cal480` submission: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal480_submission.csv`
  - `cal480` metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal480_submission_metadata.json`
  - full-train calibration picked `best_iteration=6`
- Proxy-only blend experiment between the best-cold `cal540` branch and the best-warm `cal480` branch:
  - global blend best: `0.1108328707` with `60% cal540 / 40% cal480`
  - segmented blend best: `0.1109253998`
    - cold rows: `65% cal540 + 35% cal480`
    - warm rows: `20% cal540 + 80% cal480`
- Materialized the segmented blend as the strongest submission candidate from this session:
  - artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540480_cold065_warm020_submission.csv`
  - metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540480_cold065_warm020_submission_metadata.json`
  - test-set routing counts in the blend metadata: `157,413` warm rows and `1,289,694` cold rows
- Updated recommendation:
  - `cal540` is the best single-branch submission candidate
  - the blended `cal540480_cold065_warm020` artifact is the best overall score-seeking submission candidate produced on this host so far

## Closed-Form Horizon Blend (`2026-04-13 UTC`)

- Reused the cached proxy holdout components from:
  - `outputs/lgbm_submission_proxy/warmh13_cal480_cal540_holdout_components.parquet`
- Solved the best two-model blend weights analytically instead of relying on coarse grid search.
- Results:
  - global closed-form blend: `0.1108303710`
    - `58.4888% cal540 / 41.5112% cal480`
  - cold/warm segmented closed-form blend: `0.1109220080`
    - cold rows: `64.0272% cal540 / 35.9728% cal480`
    - warm rows: `19.5834% cal540 / 80.4166% cal480`
  - cold/warm/horizon segmented closed-form blend: `0.1112328357`
    - `cold_h1`: `0.0000`
    - `cold_h3`: `0.0000`
    - `cold_h10`: `0.4398839023`
    - `cold_h25`: `0.8246525099`
    - `warm_h1`: `0.6486931013`
    - `warm_h3`: `0.7585434147`
    - `warm_h10`: `1.0000`
    - `warm_h25`: `0.0000`
- Takeaway:
  - the best available score-improvement branch on this host is no longer a single calibrated submission
  - a horizon-aware blend between `cal540` and `cal480` materially beats both the best single branch and the earlier coarse segmented blend on the pseudo-public proxy

## Horizon-Blend Submission Artifact (`2026-04-13 UTC`)

- Materialized the strongest closed-form proxy blend as a submission-format artifact:
  - artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540480_coldwarm_hblend_submission.csv`
  - metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540480_coldwarm_hblend_submission_metadata.json`
- Validation:
  - row count matches `data/test.parquet`: `1,447,107`
  - `id` values are unique and match test ordering exactly
  - blend metadata records the exact per-segment weights and segment counts used for the materialized file
- Updated recommendation:
  - submit the `cal540480_coldwarm_hblend` artifact first for live leaderboard validation
  - keep the earlier `cal540480_cold065_warm020` artifact as the fallback coarse blend if the horizon-specific blend underperforms live

## Kaggle Submission State Recheck (`2026-04-13 UTC`)

- Re-validated that authenticated Kaggle submission access is available on this cloud host despite the stale deadline assumptions captured during repo bootstrap.
- `kaggle competitions submissions -c ts-forecasting` returned prior complete submissions and public scores, including the live `0.2099` `warmh13` submission.
- Operational implication:
  - live public-score probing is available again
  - the strongest local candidate should be submitted rather than left as a proxy-only artifact

## Horizon-Blend Live Check (`2026-04-13 UTC`)

- Submitted the strongest cached proxy blend:
  - artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_cal540480_coldwarm_hblend_submission.csv`
  - message: `codex cal540480 horizon blend`
- Live result:
  - public score: `0.1800`
- Interpretation:
  - the `cal540/cal480` horizon-aware proxy blend did not transfer to the public leaderboard
  - this is now the clearest evidence in the repo that the pseudo-public tail proxy is not reliable enough to promote calibration-driven submission blends over the known `warmh13` live winner
  - keep the `0.2099` `msg020 warmh13` submission as the active public leader

## Proxy Component Export (`2026-04-13 UTC`)

- Added a reusable per-row component export path to `scripts/evaluate_lgbm_submission_proxy.py`:
  - new CLI flag: `--components-output`
  - writes aligned holdout rows with `id`, routing columns, `y_target`, `weight`, `prediction`, and `is_cold_start`
- Added regression coverage in `tests/test_evaluate_lgbm_submission_proxy.py` for:
  - writing the exported parquet file from `evaluate_tail_submission_proxy(...)`
  - exposing `--components-output` in the script help surface
- Validation:
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_lgbm_submission_proxy.py`
  - result: `7 passed in 3.96s`
- Takeaway:
  - cached holdout-component analysis is now reproducible for any submission-proxy branch instead of being limited to ad hoc one-off notebooks

## Public-Winner Family Proxy Recheck (`2026-04-13 UTC`)

- Exported the current public-winning family on the same proxy split:
  - `msg020 warmh13`:
    - summary: `outputs/lgbm_submission_proxy/msg020_warmh13_cal360_proxy.json`
    - components: `outputs/lgbm_submission_proxy/msg020_warmh13_cal360_components.parquet`
    - proxy score: `0.1062599303`
  - `msg020 warmh1`:
    - summary: `outputs/lgbm_submission_proxy/msg020_warmh1_cal360_proxy.json`
    - components: `outputs/lgbm_submission_proxy/msg020_warmh1_cal360_components.parquet`
    - proxy score: `0.1058076277`
  - `msg020 warmonly`:
    - summary: `outputs/lgbm_submission_proxy/msg020_warmonly_cal360_proxy.json`
    - components: `outputs/lgbm_submission_proxy/msg020_warmonly_cal360_components.parquet`
    - proxy score: `0.1071458916`
- Observations:
  - the proxy still prefers `warmonly`, even though `warmonly` already failed live at `0.1285`
  - `warmh13` still dominates `warmh1` on the proxy despite the two being very close live (`0.2099` vs `0.2074`)
  - pairwise proxy blends among `warmh13`, `warmh1`, and `warmonly` were not trustworthy promotion candidates because the proxy again pushes weight toward the already-failed `warmonly` branch
- Takeaway:
  - the current tail proxy is useful for cached diagnostics, but not for promoting public submissions over the live-verified `warmh13` branch

## Msg015 Control Check (`2026-04-13 UTC`)

- Exported the exact-CV-leading `msg015 warmh13` control on the same proxy split:
  - summary: `outputs/lgbm_submission_proxy/msg015_warmh13_cal360_proxy.json`
  - components: `outputs/lgbm_submission_proxy/msg015_warmh13_cal360_components.parquet`
  - proxy score: `0.1062599303`
- Direct component comparison against `msg020 warmh13` on the proxy holdout:
  - rows compared: `1,312,170`
  - changed rows: `0`
  - mean absolute prediction difference: effectively `0`
  - max absolute prediction difference: `1.33e-15`
- Interpretation:
  - the exact-CV edge for `msg015` does not show up as a different proxy prediction surface at `cal360`
  - any remaining distinction between `msg015` and the live `msg020` winner must come from the full-train calibrated submission path, not from the proxy split

## Current Working Hypothesis (`2026-04-13 UTC`)

- The `warmh13` single-policy family is now effectively plateaued:
  - same-family `reg_*` / `feature_fraction` sweeps stalled
  - broader warm-gate structures like `warmall`, `warmh1310`, and `h1325` regressed exact CV
  - proxy-driven calibration blends produced a live regression to `0.1800`
- The next materially different branch should be chosen from:
  - a submission-path comparison between the historical `msg020 warmh13` live winner and the exact-CV `msg015 warmh13` control on the real test set,
  - or a new short-horizon / cold-start specialist family motivated by the current winner’s weak exact per-horizon scores on `h=1` and `h=3`

## Submission-Path Control Reproduction (`2026-04-13 UTC`)

- Rebuilt the historical live winner locally through the checked-in submission path:
  - artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_warmh13_submission.csv`
  - metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020_ff06_bf07_ra25_rl408_warmh13_submission_metadata.json`
  - full-train calibration reproduced `best_iteration=718`
- Rebuilt the exact-CV control through the same full submission path:
  - artifact: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_submission.csv`
  - metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_submission_metadata.json`
  - full-train calibration also selected `best_iteration=718`
- Real test-set comparison between the two rebuilt submission files:
  - rows compared: `1,447,107`
  - ids matched exactly
  - changed rows: `776,383`
  - mean absolute prediction difference: `0.0042657984`
  - max absolute prediction difference: `0.5823104823`
- Interpretation:
  - unlike the proxy holdout, the full submission path does produce a materially different test surface between `msg015` and `msg020`
  - the only meaningful remaining same-family comparison is therefore on the real test artifact, not on the pseudo-public proxy

## Msg015 Vs Msg020 Slice Attribution (`2026-04-13 UTC`)

- Exact-CV summary comparison shows the `msg015` edge is concentrated entirely in `h=10/25`, not `h=1/3`:
  - overall exact score:
    - `msg015 warmh13`: `0.1605824443`
    - `msg020 warmh13`: `0.1605767426`
  - `h=1`:
    - `msg015`: `0.0388037958`
    - `msg020`: `0.0388039527`
    - `msg015` is slightly worse
  - `h=3`:
    - `msg015`: `0.0669775245`
    - `msg020`: `0.0669775923`
    - `msg015` is slightly worse
  - `h=10`:
    - `msg015`: `0.1353877647`
    - `msg020`: `0.1353846442`
    - `msg015` is better
  - `h=25`:
    - `msg015`: `0.1940280896`
    - `msg020`: `0.1940203144`
    - `msg015` is better
- Real test-set diff attribution matches that pattern:
  - cold `h=1`: identical
  - cold `h=3`: identical
  - all `h=10`: changed
  - all `h=25`: changed
  - warm `h=1/3`: changed, but exact-CV says those changes are slightly worse than `msg020`
- Takeaway:
  - a full `msg015` submission swap is not the cleanest next candidate
  - the safer same-family candidate is a horizon-routed hybrid that preserves the known-good `msg020` short-horizon behavior and only adopts the `msg015` long-horizon changes

## H10/H25 Hybrid Candidate (`2026-04-13 UTC`)

- Materialized a next-day submission candidate that routes by horizon:
  - `h=1/3` from the historical live winner `msg020 warmh13`
  - `h=10/25` from the exact-CV leader `msg015 warmh13`
- Artifact:
  - `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020015_ff06_bf07_ra25_rl408_warmh13_h1025hybrid_submission.csv`
- Metadata:
  - `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020015_ff06_bf07_ra25_rl408_warmh13_h1025hybrid_submission_metadata.json`
- Validation:
  - row count matches test: `1,447,107`
  - ids match both test ordering and the `msg020` submission exactly
  - rows sourced from `msg020`: `756,175`
  - rows sourced from `msg015`: `690,932`
  - changed rows versus `msg020`: `690,931`
  - mean absolute prediction delta versus `msg020`: `0.0041199316`
- Current recommendation:
  - keep `msg020 warmh13` as the best verified live winner
  - queue `msg020015 ... h1025hybrid` as the highest-signal next submission candidate within the current model family
  - only after that should the repo spend budget on a genuinely new short-horizon / cold-start specialist family

## Exact Fold Dump Support (`2026-04-13 UTC`)

- Added an optional per-fold scored-row dump path to `scripts/evaluate_lgbm.py`:
  - new CLI flag: `--scored-output-dir`
  - each fold now optionally writes a parquet file containing the scored validation rows after cold-start annotation
- Added regression coverage in `tests/test_evaluate_lgbm.py` for the parquet writer helper.
- Validation:
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_lgbm.py -k 'write_scored_fold_frame'`
  - result: `1 passed, 18 deselected`
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_lgbm.py`
  - result: `19 passed`
- Purpose:
  - this closes the tooling gap that prevented exact offline scoring of routed hybrids built from two saved LGBM parents

## Exact H10/H25 Hybrid Check (`2026-04-13 UTC`)

- Re-ran the two active `warmh13` parents with fold-score dumps enabled:
  - `msg015`: `outputs/hybrid_exact_scored/msg015`
  - `msg020`: `outputs/hybrid_exact_scored/msg020`
- Exact rerun results from those dumped folds:
  - `msg015 warmh13`: `0.1605847605`
  - `msg020 warmh13`: `0.1605806370`
- Scored the routed hybrid exactly by taking:
  - `h=1/3` predictions from `msg020`
  - `h=10/25` predictions from `msg015`
- Exact hybrid artifact:
  - summary: `outputs/hybrid_exact_scored/msg020015_h1025_hybrid_exact_summary.json`
- Exact hybrid result:
  - overall score: `0.1605848030`
  - cold-start score: `0.1720968085`
  - warm-start score: `0.1283100668`
- Interpretation:
  - the `h1025` horizon-routed hybrid is now the strongest validated candidate inside the current `warmh13` family
  - it is only a tiny offline lift, but it beats both rerun parents while preserving the live winner’s `h=1/3` branch
  - this is now the highest-signal next submission candidate when the daily submission slot reopens

## Exact Horizon Route Search (`2026-04-13 UTC`)

- Exhaustively scored all `16` horizon-routing combinations between the two rerun parents using the dumped exact fold rows:
  - route search summary: `outputs/hybrid_exact_scored/msg020015_horizon_route_search.json`
- Best route:
  - use `msg020` for `h=1/3/10`
  - use `msg015` only for `h=25`
- Exact result for the winning route:
  - overall score: `0.1605861611`
  - cold-start score: `0.1720985876`
  - warm-start score: `0.1283100946`
- Strongest alternatives from the same search:
  - `msg015` on `[1,25]`: `0.1605861605`
  - `msg015` on `[3,25]`: `0.1605861191`
  - `msg015` on `[1,3,25]`: `0.1605861185`
  - prior `h10/25` hybrid: `0.1605848030`
- Materialized submission candidate:
  - submission: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020015_ff06_bf07_ra25_rl408_warmh13_h25hybrid_submission.csv`
  - metadata: `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg020015_ff06_bf07_ra25_rl408_warmh13_h25hybrid_submission_metadata.json`
- Validation:
  - row count matches test: `1,447,107`
  - ids match both test ordering and the `msg020` submission exactly
  - changed rows versus `msg020`: `328,874`
  - mean absolute prediction delta versus `msg020`: `0.0020849035`
- Interpretation:
  - the current same-family optimum is no longer the `h10/25` route; it is `msg015` only on `h=25`
  - this likely captures the only robust slice where `msg015`’s exact-CV edge survives while preserving the live winner’s stronger short-horizon behavior
  - once the next submission window opens, this `h25` hybrid is the highest-signal within-family candidate; after that, more progress likely requires a genuinely different model family

## Baseline Fold Dump Support (`2026-04-13 UTC`)

- Added the same exact scored-row export pattern to `scripts/evaluate_baselines.py`:
  - new helper: `write_scored_fold_frame(...)`
  - new CLI flag: `--scored-output-dir`
  - each baseline fold can now write its scored validation rows as parquet for exact posthoc hybrid search
- Added regression coverage in `tests/test_evaluate_baselines.py` for the new parquet writer helper.
- Targeted validation:
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_baselines.py -k write_scored_fold_frame`
  - result: `1 passed, 16 deselected`
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_baselines.py`
  - result: `17 passed`
- Purpose:
  - this closes the last tooling gap for exact cross-family routing between the live `warmh13` LGBM family and the older baseline / residual expert families

## Cross-Family Exact Baseline Reruns (`2026-04-13 UTC`)

- Re-ran the strongest already-supported non-LGBM baselines with exact fold dumps enabled:
  - incumbent recent-delta baseline:
    - `outputs/advanced_models/cross_family_exact_20260413_incumbent/forward_cv_h360_f4_s360_code_sub_code/summary.json`
    - exact score: `0.1451839276`
  - strict-OOF ridge residual winner:
    - `outputs/advanced_models/cross_family_exact_20260413_ridge/forward_cv_h360_f4_s360_code_sub_code/summary.json`
    - exact score: `0.1453567994`
  - standalone longwatch residual expert:
    - `outputs/advanced_models/cross_family_exact_20260413_longwatch_raw/forward_cv_h360_f4_s360_code_sub_code/summary.json`
    - exact score: `0.1380775138`
  - conditional longwatch blend:
    - `outputs/advanced_models/cross_family_exact_20260413_longwatch_blend/forward_cv_h360_f4_s360_code_sub_code/summary.json`
    - exact score: `0.1446841822`
- Interpretation:
  - the reruns match the earlier repo picture: ridge remains the best baseline-family alternative, the longwatch raw expert stays clearly closed, and the conditional blend still fails to beat the simpler ridge path on canonical exact CV

## Cross-Family Route Search (`2026-04-13 UTC`)

- Scored exact routed hybrids between the current same-family leader
  - `msg020` on `h=1/3/10` plus `msg015` on `h=25`
  - and the strongest baseline-family alternatives using the saved scored fold rows
- Segment search over `cold/warm x horizon` between:
  - current `h25` LGBM hybrid
  - `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1`
  - `smoothed_stable_horizon_blend_last_target_recent_delta_k7_h13_pentaslice_h3_beta_up_v1`
  - summary: `outputs/hybrid_exact_scored/msg020015_vs_ridge_incumbent_segment_route_search.json`
- Result:
  - best route is still the plain `h25` LGBM hybrid at `0.1605861611`
  - the only baseline-family “improvement” was an exact tie from swapping ridge onto cold `h=1` and/or cold `h=3`, with no positive lift beyond floating-point identity
- Watchlist route search over:
  - default = current `h25` LGBM hybrid
  - optional swaps on `osjl3a7y_long_core`, `x9bz68vq_h10_core`, and `x9bz68vq_h25_core`
  - candidates = current `h25` LGBM hybrid, ridge, and `winner_plus_longwatch_expert_blend_v1`
  - summary: `outputs/hybrid_exact_scored/msg020015_watchlist_route_search.json`
- Result:
  - best route again leaves all three watchlists on the plain `h25` LGBM hybrid at `0.1605861611`
  - the closest alternative, swapping ridge only onto `x9bz68vq_h10_core`, regressed to `0.1603833746`
- Interpretation:
  - the current repo leader survives both the broad segment search and the targeted longwatch-slice search
  - the older baseline and longwatch families are now closed not just as standalone models, but also as routed add-ons to the promoted `warmh13` branch
  - the next meaningful unattended progress likely requires a new model surface, not more routing between the existing saved experts

## Alternative Residual Learner Screen (`2026-04-13 UTC`)

- Opened a genuinely new model-surface branch by evaluating non-ridge normalized residual experts on top of the strict-OOF ridge incumbent:
  - base summary: `outputs/advanced_models/cross_family_exact_20260413_ridge/forward_cv_h360_f4_s360_code_sub_code/summary.json`
  - base baseline: `ridge_oofnorm_pentaslice_h13_s008_q85_a50_unweighted_v1`
- Exact standalone screens:
  - Huber short-horizon expert:
    - `outputs/residual_models_surface_20260413/forward_cv_h360_f4_s360_code_sub_code_huber_h13/summary.json`
    - config: `huber_oofnorm_pentaslice_h13_s008_q85_a1_eps135_i200_v1`
    - exact score: `0.1455316406`
  - HistGBM short-horizon expert:
    - `outputs/residual_models_surface_20260413/forward_cv_h360_f4_s360_code_sub_code_histgbm_h13/summary.json`
    - config: `histgbm_oofnorm_pentaslice_h13_s008_q85_lr005_md6_i200_leaf200_v1`
    - exact score: `0.1456269886`
  - Huber `h=25` expert:
    - `outputs/residual_models_surface_20260413/forward_cv_h360_f4_s360_code_sub_code_huber_h25/summary.json`
    - config: `huber_oofnorm_allsub_h25_s005_q80_a1_eps135_i200_v1`
    - exact score: `0.1468595049`
  - HistGBM `h=25` expert:
    - `outputs/residual_models_surface_20260413/forward_cv_h360_f4_s360_code_sub_code_histgbm_h25/summary.json`
    - config: `histgbm_oofnorm_allsub_h25_s005_q80_lr005_md6_i200_leaf200_v1`
    - exact score: `0.1460603513`
- Interpretation:
  - all four new learners beat the old ridge base family on exact CV
  - the strongest standalone short-horizon expert is HistGBM, while the strongest standalone `h=25` expert is Huber
  - none of the standalone scores threaten the promoted LGBM family, so the only viable promotion path is routed use on the specific slices where they beat the current leader

## New-Surface Exact Route Search (`2026-04-13 UTC`)

- Tested the new residual experts as routed add-ons to the current same-family leader
  - base route: `msg020` on `h=1/3/10` plus `msg015` on `h=25`
  - current exact score: `0.1605861611`
- `h=25` expert checks:
  - `outputs/hybrid_exact_scored/msg020015_plus_huber_h25_exact_summary.json`
    - route: replace `h=25` rows with the Huber `h=25` expert
    - exact score: `0.1509288236`
  - `outputs/hybrid_exact_scored/msg020015_plus_histgbm_h25_exact_summary.json`
    - route: replace `h=25` rows with the HistGBM `h=25` expert
    - exact score: `0.1501513298`
  - decision: both `h=25` experts are closed for promotion; they beat ridge but lose badly to the promoted LGBM `h=25` branch
- Short-horizon expert checks:
  - `outputs/hybrid_exact_scored/msg020015_plus_huber_h13_route_search.json`
    - best route: use Huber on cold `h=1` and cold `h=3`, keep the current LGBM route elsewhere
    - exact score: `0.1606472618`
  - `outputs/hybrid_exact_scored/msg020015_plus_histgbm_h13_route_search.json`
    - best route: use HistGBM on all `h=1` rows plus cold `h=3`, keep the current LGBM route elsewhere
    - exact score: `0.1606880176`
  - `outputs/hybrid_exact_scored/msg020015_plus_huber_hist_h13_route_search.json`
    - mixed Huber/Hist search on `cold/warm x {1,3}`
    - no mixed route beat the pure HistGBM short-horizon route
- Interpretation:
  - the first materially better exact route after the `msg020/msg015 h25` hybrid is now the HistGBM short-horizon expert
  - this is the strongest offline result in the repo since the `warmh13` family plateaued
  - the best rule is simple enough to package safely: HistGBM on all `h=1`, HistGBM on cold `h=3`, and the existing LGBM `h25` hybrid elsewhere

## Short-Horizon Expert Registry Exposure (`2026-04-13 UTC`)

- Promoted the new short-horizon residual experts into `src/ts_forecasting/baseline_registry.py` as opt-in baselines:
  - `huber_oofnorm_pentaslice_h13_s008_q85_a1_eps135_i200_v1`
  - `histgbm_oofnorm_pentaslice_h13_s008_q85_lr005_md6_i200_leaf200_v1`
- This makes both experts accessible through the normal builder / prediction path rather than only through ad hoc inline config injection.
- Added regression coverage in `tests/test_evaluate_baselines.py` to assert that both configs are exposed and preserve their key knobs under both `scripts/evaluate_baselines.py` and `scripts/predict_baseline.py`.
- Validation:
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_baselines.py -k non_ridge_residual_variants`
  - result: `1 passed, 17 deselected`
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_baselines.py`
  - result: `18 passed`
  - `. .venv/bin/activate && python -m pytest -q`
  - result: `106 passed, 1 warning`
- Status:
  - full-train HistGBM packaging for the new best route is running separately; the exact routing evidence above is already final, but the submission artifact generation has not finished yet at this log point

## HistGBM Route Materialization (`2026-04-13 UTC`)

- Materialized the new best offline route on the real test set:
  - HistGBM expert submission:
    - `outputs/submissions/histgbm_oofnorm_pentaslice_h13_s008_q85_lr005_md6_i200_leaf200_v1_submission.csv`
  - routed candidate submission:
    - `outputs/submissions/lgbm_msg020015_h25_plus_histgbm_h13_h1all_coldh3_submission.csv`
  - metadata:
    - `outputs/submissions/lgbm_msg020015_h25_plus_histgbm_h13_h1all_coldh3_submission_metadata.json`
- Final routing rule:
  - use `histgbm_oofnorm_pentaslice_h13_s008_q85_lr005_md6_i200_leaf200_v1` on all `h=1` rows
  - use the same HistGBM expert on cold `code_sub_code` rows for `h=3`
  - use the existing `msg020/msg015 h25` LGBM hybrid elsewhere
- Test-set route counts:
  - routed rows: `713,795 / 1,447,107` (`49.33%`)
  - `h=1` rows routed: `379,617`
  - cold `h=3` rows routed: `334,178`
- Integrity checks:
  - the final routed submission has `1,447,107` rows, `1,447,107` unique ids, and no missing predictions
  - the final routed submission matches the test-set id ordering exactly
- Packaging note:
  - the raw HistGBM expert submission came back in a different id order than the existing LGBM submission, so the final hybrid was assembled by explicit `id` mapping rather than positional replacement
- Current recommendation:
  - when the next submission slot opens, the first priority should be `lgbm_msg020015_h25_plus_histgbm_h13_h1all_coldh3_submission.csv`
  - this is now the strongest offline candidate in the repo after the `warmh13` family plateau

## Causal History Transformer Branch (`2026-04-13 UTC`)

- Opened a more drastic model-surface branch by adding a stateful `CausalHistoryTransformer` in `src/ts_forecasting/sequential.py`.
- Purpose:
  - generate leakage-safe history features from prior rows only,
  - preserve same-`ts_index` causality inside each batch,
  - make that feature branch reusable through the existing baseline / residual stack.
- Registry and CLI wiring:
  - added `build_transformer(...)` and transformer-aware required-column resolution in `src/ts_forecasting/baseline_registry.py`
  - switched `scripts/evaluate_baselines.py` and `scripts/predict_baseline.py` from hardcoded `IdentityTransformer()` to registry-built transformers
  - exposed an opt-in baseline config:
    - `histgbm_oofnorm_pentaslice_h13_s008_q85_lr005_md6_i200_leaf200_causal_history_v1`
- Regression coverage:
  - `tests/test_sequential.py`
    - causal state carryover / same-time safety
    - no DataFrame fragmentation warning under wide history expansion
  - `tests/test_evaluate_baselines.py`
    - evaluator wiring uses configured transformers
    - new causal HistGBM config is exposed and preserves transformer knobs
- Validation:
  - `. .venv/bin/activate && python -m pytest -q tests/test_sequential.py`
    - result: `6 passed`
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_baselines.py`
    - result: `20 passed`
  - `. .venv/bin/activate && python -m pytest -q`
    - result: `110 passed, 1 warning`
- Performance fix during rollout:
  - the first transformer implementation inserted derived columns one-by-one, which triggered heavy pandas fragmentation warnings and inflated memory/runtime
  - replaced that path with preallocated numpy arrays plus one final concat
  - decision: treat any early causal probe launched before this fix as stale and restart it on the repaired transformer

## Active Causal Probe Matrix (`2026-04-13 UTC`)

- Closed the earlier split HistGBM `h=1` / `h=3` exact reruns after they consumed the box and still had only produced fold `01`; they were deprioritized in favor of the new causal-history surface.
- Active one-fold holdout probes on the repaired transformer:
  - `outputs/advanced_models/causal_history_probe_20260413_fixed`
    - existing short-horizon HistGBM expert vs full causal-history variant (`last + mean + delta`)
  - `outputs/advanced_models/causal_history_h25_probe_20260413_fixed`
    - ad hoc `h=25` HistGBM expert vs full causal-history variant
  - `outputs/advanced_models/causal_history_last_h13_probe_20260413_fixed`
    - short-horizon HistGBM expert vs lean causal-history variant (`last` only)
  - `outputs/advanced_models/causal_history_last_h25_probe_20260413_fixed`
    - `h=25` HistGBM expert vs lean causal-history variant (`last` only)
  - `outputs/advanced_models/causal_history_last_huber_h25_probe_20260413_fixed`
    - `h=25` Huber expert vs lean causal-history variant (`last` only)
- Decision rule:
  - if any repaired holdout probe shows a real lift over its non-causal parent, promote only that winner into exact `4`-fold CV with scored-row dumps
  - if all five repaired probes fail or stay neutral, the next drastic branch should move the same causal-history idea into the main LGBM pipeline rather than iterating further inside the residual-baseline stack

## Causal Holdout Readout (`2026-04-13 UTC`)

- Repaired one-fold holdout results on the lean `history_features=["last"]` branch:
  - short-horizon HistGBM:
    - baseline `histgbm_oofnorm_pentaslice_h13_s008_q85_lr005_md6_i200_leaf200_v1`: `0.1055633015`
    - causal-last variant: `0.1052800187`
    - decision: closed, causal history regressed
  - short-horizon Huber:
    - baseline `huber_oofnorm_pentaslice_h13_s008_q85_a1_eps135_i200_v1`: `0.1058537590`
    - causal-last variant: `0.1057745063`
    - decision: closed, causal history regressed
  - `h=25` HistGBM:
    - baseline `histgbm_oofnorm_allsub_h25_s005_q80_lr005_md6_i200_leaf200_v1`: `0.1087746710`
    - causal-last variant: `0.1096653777`
    - decision: promoted into exact `4`-fold CV
  - `h=25` Huber:
    - baseline `huber_oofnorm_allsub_h25_s005_q80_a1_eps135_i200_v1`: `0.1142737405`
    - causal-last variant: `0.1155343551`
    - decision: promoted into exact `4`-fold CV
- Interpretation:
  - causal history is not a general short-horizon lift
  - the only positive signal is on the larger `h=25` error bucket, which is exactly where a bigger jump is more plausible

## Exact H25 Causal Promotion (`2026-04-13 UTC`)

- Launched exact `4`-fold CV plus scored-row dumps for the two promoted `h=25` causal-last variants:
  - HistGBM route candidate:
    - output dir: `outputs/advanced_models/causal_history_exact_h25_hist_20260413`
    - scored dir: `outputs/hybrid_exact_scored/causal_history_exact_h25_hist_20260413`
  - Huber route candidate:
    - output dir: `outputs/advanced_models/causal_history_exact_h25_huber_20260413`
    - scored dir: `outputs/hybrid_exact_scored/causal_history_exact_h25_huber_20260413`
- Status at log time:
  - both exact runs had started and written `fold_01` prediction artifacts
  - no exact summary had landed yet
- Next action:
  - if either exact run improves on its non-causal parent, splice its `h=25` fold rows onto the current `msg020/msg015 h25` hybrid and score the routed result exactly before generating any new submission artifact

### Exact rerun cleanup

- The first exact reruns were accidentally launched with both the old parent and the new causal candidate in each process, which wasted time by re-scoring already-known parent baselines.
- Killed those pairwise exact jobs and relaunched candidate-only exact CV:
  - HistGBM candidate only:
    - `histgbm_oofnorm_allsub_h25_s005_q80_lr005_md6_i200_leaf200_causal_last_v1`
  - Huber candidate only:
    - `huber_oofnorm_allsub_h25_s005_q80_a1_eps135_i200_causal_last_v1`
- Output roots stayed the same:
  - `outputs/advanced_models/causal_history_exact_h25_hist_20260413`
  - `outputs/advanced_models/causal_history_exact_h25_huber_20260413`
  - `outputs/hybrid_exact_scored/causal_history_exact_h25_hist_20260413`
  - `outputs/hybrid_exact_scored/causal_history_exact_h25_huber_20260413`
- Important note:
  - those directories still contain stale `fold_01` parent artifacts from the aborted pairwise attempt
  - only files with the `*_causal_last_v1_*` naming pattern should be treated as authoritative for the relaunched candidate-only exact runs

## LGBM Causal Seam (`2026-04-13 UTC`)

- Added the same causal-history seam to the main LGBM stack:
  - `scripts/evaluate_lgbm.py`
    - `run_fold()` now imports `build_transformer(...)` and can apply a configured transformer before LGBM fit / predict
  - `scripts/predict_lgbm.py`
    - `predict_residual_lgbm()` and `calibrate_residual_lgbm_n_estimators()` now apply the configured transformer on the causal train-to-future path before the LGBM residual fit
- Current behavior:
  - existing runs remain unchanged unless the incumbent config passed into the LGBM path carries a `transformer` block
  - this keeps the current promoted path stable while making the next causal LGBM branch available immediately
- Validation:
  - `. .venv/bin/activate && python -m pytest -q tests/test_evaluate_lgbm.py tests/test_predict_lgbm.py`
  - result: `29 passed`

## Causal Warmh13 LGBM Probe (`2026-04-13 UTC`)

- Opened a direct one-fold holdout probe on the main promoted `warmh13` LightGBM family using the new causal-history seam:
  - output root: `outputs/lgbm_models_causal_history_probe_20260413`
  - setup:
    - current `warmh13` parameter center (`leaf31 / lr0.02 / mc520 / ff0.6 / bf0.7 / ra2.5 / rl4.08`)
    - residual horizons `1 3 10 25`
    - warm-only gate on horizons `1 3`
    - incumbent config temporarily wrapped with a causal-history transformer using `history_features=["last"]`
- Runtime observation:
  - the expensive incumbent OOF phase completed, and the probe was in the downstream LightGBM fit stage at log time
- Purpose:
  - determine whether causal-history features belong only in the `h=25` specialist branch or can move the main `warmh13` LGBM family as well

## Causal Branch Triage (`2026-04-13 UTC`)

- The causal-history `warmh13` LGBM probe finished and was decisively negative:
  - summary: `outputs/lgbm_models_causal_history_probe_20260413/forward_cv_h360_f1_s360_code_sub_code/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_causallast_warmh13_summary.json`
  - one-fold score: `0.1093972019`
  - matching non-causal control on the same validation window (`fold_04_train_3241_valid_3242_3601`) from `outputs/lgbm_models_causal_20260413/forward_cv_h360_f4_s360_code_sub_code/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_warmh13_warmh13_summary.json`: `0.1662215478`
  - decision: close the causal-history branch on the main `warmh13` LightGBM family
- While the `h=25` causal-last exact reruns were still in progress, checked whether they were already helping on a completed routed fold:
  - current routed control on fold `02` (`msg020` everywhere except `msg015` on `h=25`): `0.1028923061`
  - replacing fold `02` `h=25` rows with the HistGBM causal-last candidate: `0.0977132900`
  - replacing fold `02` `h=25` rows with the Huber causal-last candidate: `0.0931513417`
  - fold `01` was unchanged versus the old parent for both candidates
  - decision: the current `h=25` causal-last exact reruns were not on pace to beat the routed winner, so both exact jobs were killed early instead of spending more wall time on a losing route
- Freed the host and redirected the budget to new one-fold `h=25` holdout probes, because `h=25` remains the only causal branch with any positive signal:
  - `outputs/advanced_models/causal_h25_hist_structural_probe_20260413`
    - active HistGBM sweep around the causal-last control, testing:
      - coarser causal grouping `["code", "horizon"]`
      - finer warm-aware grouping `["code", "sub_code", "sub_category", "horizon"]`
      - richer history `["last", "mean"]`
      - a higher-capacity booster (`lr=0.03`, `max_depth=8`, `max_iter=300`, `min_samples_leaf=100`, `max_train_rows=260000`)
  - `outputs/advanced_models/causal_h25_huber_structural_probe_20260413`
    - active Huber sweep around the causal-last control, testing:
      - coarser causal grouping `["code", "horizon"]`
      - finer warm-aware grouping `["code", "sub_code", "sub_category", "horizon"]`
      - richer history `["last", "mean"]`
      - a stronger robust fit (`alpha=0.75`, `epsilon=1.2`, `max_iter=400`, `max_train_rows=260000`)
  - `outputs/advanced_models/causal_h25_priority_probe_20260413`
    - active second-wave probe focused on the most plausible bigger jumps:
      - HistGBM with `["last", "mean", "delta"]` plus stronger correction (`scale=0.08`, `clip=0.85`)
      - HistGBM with a longer residual window (`residual_train_steps=1080`, `scale=0.08`, `clip=0.85`)
      - HistGBM higher-capacity correction (`lr=0.03`, `max_iter=400`, `min_samples_leaf=100`)
      - Huber with `["last", "mean", "delta"]` plus stronger correction (`scale=0.08`, `clip=0.85`)
      - Huber with stronger robust correction (`alpha=0.5`, `epsilon=1.2`, `max_iter=400`, `scale=0.08`, `clip=0.85`)
      - Huber with a longer residual window (`residual_train_steps=1080`, `scale=0.08`, `clip=0.85`)
- Current interpretation:
  - causal history is closed on the main LGBM route
  - the original `h=25` causal-last candidate is not strong enough to displace the routed winner directly
  - the next meaningful chance of a larger jump is a more aggressive `h=25` specialist, not more global `warmh13` tuning

## Incumbent-Prediction History Probe (`2026-04-13 UTC`)

- Opened a new one-fold LightGBM probe that keeps the current `warmh13` residual center but applies the causal-history seam only to `incumbent_pred`, not to the raw feature surface:
  - output root: `outputs/lgbm_models_incpred_history_probe_20260413`
  - transformer used on the LGBM input frame:
    - `source_columns=["incumbent_pred"]`
    - `level_columns=["code", "sub_category", "horizon"]`
    - `history_features=["last", "mean", "delta"]`
- Result:
  - summary: `outputs/lgbm_models_incpred_history_probe_20260413/forward_cv_h360_f1_s360_code_sub_code/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_incpredhist_warmh13_summary.json`
  - one-fold score: `0.1662032583`
  - matching non-causal control on the same holdout window: `0.1662215478`
  - delta: `-0.0000182895`
- Interpretation:
  - unlike the raw-feature causal-history branch, causal lag features over `incumbent_pred` are essentially neutral rather than catastrophic
  - this is the first causal branch that plausibly captures the target-overlap structure without a large regression, so it remains open
- Immediate follow-up probes launched off the same one-fold holdout:
  - `..._incpred_codeh_lastmean_warmh13`
    - `level_columns=["code", "horizon"]`
    - `history_features=["last", "mean"]`
  - `..._incpred_codesubh_last_warmh13`
    - `level_columns=["code", "sub_code", "horizon"]`
    - `history_features=["last"]`
- Decision rule:
  - if either follow-up clears the control on the one-fold holdout, promote that incumbent-history variant into a broader exact check before reopening more raw-feature or `h=25` specialist complexity
- Follow-up readout:
  - `..._incpred_codeh_lastmean_warmh13`: `0.1662188402`
  - `..._incpred_codesubh_last_warmh13`: `0.1662131848`
  - `..._incpred_codeh_last_warmh13`: `0.1661976260`
  - `..._incpred_codecath_lastmean_warmh13`: `0.1662198121`
  - `..._incpred_codecath_last_warmh13`: `0.1661975198`
- Updated interpretation:
  - the best incumbent-history variant is now `code / sub_category / horizon` with `history_features=["last", "mean"]`
  - it still trails the non-causal control, but only by `0.0000017357`, which is the closest any causal-history LightGBM branch has come to parity
  - this keeps the incumbent-history seam open while the raw-feature causal branch stays closed
- Additional targeted follow-up launched:
  - `..._h1025_..._incpred_codecath_lastmean`
    - same best incumbent-history transformer, but residual LightGBM only on horizons `10` and `25`
  - `..._h25_..._incpred_codecath_lastmean`
    - same best incumbent-history transformer, but residual LightGBM only on horizon `25`
- Rationale:
  - the overlap signal may be concentrated in long horizons and diluted when the same model is also correcting `h=1/3`
- Exact promotion:
  - because the best one-fold incumbent-history candidate is effectively tied with the control, launched an exact `4`-fold rerun with scored-row dumps:
    - output dir: `outputs/lgbm_models_incpred_history_exact_20260413`
    - scored dir: `outputs/hybrid_exact_scored/incpred_codecath_lastmean_20260413`
    - candidate: `lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_incpred_codecath_lastmean_warmh13`
  - decision rule:
    - if the exact rerun beats or ties the current all-horizon control closely enough to improve routed diagnostics, keep this seam open for submission-style or routing promotion
    - if the exact rerun regresses clearly, close the incumbent-history branch and fall back to the slower `h=25` structural sweeps
- Exact rerun result:
  - summary: `outputs/lgbm_models_incpred_history_exact_20260413/forward_cv_h360_f4_s360_code_sub_code/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_incpred_codecath_lastmean_warmh13_summary.json`
  - exact score: `0.1605854514`
  - current all-horizon `msg015 warmh13` control: `0.1605847605`
  - interpretation:
    - the incumbent-history candidate is slightly better than the plain all-horizon control on exact CV
    - by fold, the lift came mostly from folds `03` and `04`; fold `01` was identical and fold `02` was neutral
- Routed overlay search against the actual repo-best exact route:
  - base route is still the short-horizon HistGBM expert winner:
    - HistGBM on all `h=1`
    - HistGBM on cold `h=3`
    - `msg020` on the remaining `h=3/10`
    - `msg015` on `h=25`
    - exact score: `0.1606880176`
  - searched exact overlays from the incumbent-history candidate on the remaining slices and saved:
    - `outputs/hybrid_exact_scored/incpred_codecath_lastmean_route_search.json`
  - best exact route found:
    - overlay the incumbent-history candidate only on `cold h10`
    - exact score: `0.1606895792`
  - refined `cold h10` search:
    - subcategory-restricted search saved at `outputs/hybrid_exact_scored/incpred_codecath_lastmean_cold_h10_subcat_route_search.json`
    - best subcategory mask: `DPPUO5X2`, `PHHHVYZI`, `PZ9S1Z4V`
    - exact score: `0.1606895830`
  - code-level greedy refinement inside that best subcategory mask:
    - saved at `outputs/hybrid_exact_scored/incpred_codecath_lastmean_cold_h10_subcat_code_greedy.json`
    - best practical route: overlay the incumbent-history candidate only on `cold h10` rows where:
      - `code == 10BAVIDU`
      - `sub_category in {DPPUO5X2, PHHHVYZI, PZ9S1Z4V}`
    - exact score: `0.1606896704`
    - matching full-test mask size against `test.parquet`: `4,114` rows
  - close follow-ups:
    - `warm h3 + cold h10`: `0.1606895420`
    - all `h10`: `0.1606894699`
  - decision:
    - the incumbent-history branch is now the new exact repo leader, but only as a very narrow routed overlay on top of the short-horizon HistGBM winner
    - do not promote the incumbent-history policy as a full replacement; keep it as a targeted `cold h10` specialist focused on the `10BAVIDU` slice above
- Submission-style follow-up opened:
  - generating a full-test incumbent-history prediction artifact so the new `cold h10` route can be materialized on top of `outputs/submissions/lgbm_msg020015_h25_plus_histgbm_h13_h1all_coldh3_submission.csv`
  - full-test incumbent-history artifact completed:
    - `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_incpred_codecath_lastmean_submission.csv`
    - metadata:
      - `outputs/submissions/lgbm_residual_weighted_v2_h131025_leaf31_lr002_mc520_msg015_ff06_bf07_ra25_rl408_incpred_codecath_lastmean_submission_metadata.json`
  - routed candidate materialized:
    - `outputs/submissions/lgbm_msg020015_h25_plus_histgbm_h13_h1all_coldh3_plus_incpred_coldh10_10BAVIDU_core_submission.csv`
    - metadata:
      - `outputs/submissions/lgbm_msg020015_h25_plus_histgbm_h13_h1all_coldh3_plus_incpred_coldh10_10BAVIDU_core_submission_metadata.json`
  - practical caveat:
    - the exact route gain is real on CV, but on full-test inference the routed overlay changed only `4,114` rows and the mean absolute prediction delta on those rows was `4.8216e-11`
    - interpretation: this branch currently looks like a very weak or possibly non-transferable full-train effect, so do not expect a material public-LB jump from this candidate alone

## H25 Last-Mean Promotion (`2026-04-13 UTC`)

- The slow structural `h=25` probes eventually wrote the `history_features=["last","mean"]` variants, and both beat the earlier causal-last controls on the one-fold holdout:
  - HistGBM:
    - prior causal-last holdout: `0.1096653777`
    - causal-lastmean holdout: `0.1117708830`
  - Huber:
    - prior causal-last holdout: `0.1155343551`
    - causal-lastmean holdout: `0.1164274165`
- Decision:
  - these are the first meaningful `h=25` improvements after the earlier causal-last exact reruns failed, so both were promoted into fresh exact candidate-only reruns with scored-row dumps:
    - HistGBM:
      - output dir: `outputs/advanced_models/causal_history_exact_h25_hist_lastmean_20260413`
      - scored dir: `outputs/hybrid_exact_scored/causal_history_exact_h25_hist_lastmean_20260413`
    - Huber:
      - output dir: `outputs/advanced_models/causal_history_exact_h25_huber_lastmean_20260413`
      - scored dir: `outputs/hybrid_exact_scored/causal_history_exact_h25_huber_lastmean_20260413`

## Live Kaggle Snapshot (`2026-04-13 UTC`)

- Confirmed via authenticated Kaggle CLI that submission history is still queryable on this host:
  - current best public score in our own submission history remains `0.2099`
  - the latest failed blend retry remains `0.1800`
- Current top public leaderboard score from `kaggle competitions leaderboard -c ts-forecasting --show`:
  - first place: `0.8422`
  - second place: `0.6761`
- Interpretation:
  - the `0.8+` target is real on the public board, not a stale placeholder
  - our current leakage-safe branch is still far from that frontier, so larger structural moves remain justified
